Finish the DS1 theme system in DiscountDora.

WHAT'S DONE (don't redo):
- `web_app/src/css/tokens.scss` declares the full token catalogue
  (raw palette, semantic surfaces/text/borders, overlays, spacing,
  radius, font-size scale, elevation). HSL-in-sRGB, `color-scheme:
  light` default.
- `web_app/src/css/themes.scss` defines 5 named themes via
  `[data-theme="lemon-tart"|"sourdough"|"blueberry"|"avocado"|
  "midnight-snack"]`. Midnight Snack overrides overlays for dark
  mode.
- `web_app/src/css/quasar.variables.scss` ships Lemon Tart as the
  compile-time Quasar palette.
- `web_app/src/css/colours.scss` legacy `--q-*` shim now points at
  the matching DS1 tokens.
- `web_app/src/services/themeService.ts` exports a `THEMES`
  catalogue keyed by theme name (`label`, `blurb`, `swatch`,
  `palette`, `isDark`). `applyForUser` sets `data-theme` on `<html>`
  AND calls `setCssVar()` per palette key so Quasar components and
  our CSS tokens stay in sync. Legacy `light` / `dark` map to
  Lemon Tart / Midnight Snack.
- `web_app/src/models/auth.ts` `ThemePreference` includes all 5
  named themes + legacy `light`/`dark` + `system`.
- `dora_api/domain/entities/user.py` `ALLOWED_THEMES` includes all
  named values; `update_me` already gates against it.
- `web_app/src/pages/settings/PreferencesSettings.vue` renders a
  card-grid picker fed by `SELECTABLE_THEME_KEYS` + `THEMES` with
  swatch previews.
- Per-component sweep: 103 hex/rgba → token replacements across 31
  files (only within `<style>` blocks). Merchant logos, data-viz
  palettes (`usePriceHistoryPalette.ts`, StockMap node rectangles,
  Dashboard / Reports / SubstitutesGraph accent palettes), and the
  TrendSparkline up/down indicator were deliberately skipped.

WHAT'S LEFT (your job):
1. **Visual audit, all 6 themes** (System included). Boot the SPA,
   sign in, click through every top-level page + every Settings tab
   while cycling themes from the picker. Hunt for:
   - Text-on-background contrast regressions, especially on
     Midnight Snack (white-text-on-light or dark-text-on-dark).
   - Cards / banners that ended up the same colour as their parent
     surface (white on white, etc.). Fix locally — either use
     `--surface-elevated` / `--surface-sunken` instead of
     `--surface-component`, or add a `border: 1px solid
     var(--border-default)`.
   - Quasar `text-grey-*` and `bg-grey-*` utility classes that
     are still pinned to material defaults regardless of theme;
     prefer the tokens.
   Make targeted edits as you go — don't sweep blindly.

2. **Residual hex still in the codebase.** Run `grep -rE "#[0-9a-fA-F]{3,6}|rgba?\("
   web_app/src/{pages,components}`. Audit each remaining hit:
   - If it's brand identity / data-viz / fullscreen-immersive
     (already-intentional category) → leave it.
   - If it's a per-component surface/text/border that should
     theme → swap to a token.
   - Pay special attention to legacy `rgba(23, 176, 115, ...)`
     greens (left over from the pre-Lemon-Tart palette) — those
     should go.

3. **Lazy migration of legacy `light`/`dark` rows.** On the next
   successful preference save by a user whose stored theme is
   `light` or `dark`, the backend should rewrite to `lemon-tart` /
   `midnight-snack` so the legacy values eventually drain. Add to
   `dora_api/features/auth/update_me.py`.

4. **DTO + frontend type cleanup.** Once (3) is in, drop the
   `'light' | 'dark'` cases from `ThemePreference` in
   `web_app/src/models/auth.ts` and remove the legacy mapping in
   `themeService.ts`'s `resolveThemeKey()`. Defer this until the
   migration has had a chance to run in any real install.

5. **Persist + restore on logout.** Confirm that
   `themeService.applyForUser` is called on `authStore.loginAsync`,
   `registerAsync`, and `updateMeAsync`. On `logoutAsync` the SPA
   should fall back to `applyTheme()` (system default) — verify or
   add.

6. **Test the picker end-to-end.** Pick each theme, refresh the
   page, confirm the picked theme survives. Confirm System honours
   `prefers-color-scheme` flips during the session (toggle OS dark
   mode while on `system`).

7. **CHANGELOG entry** under `[Unreleased]` summarising what you
   shipped. Match the prose style of nearby entries.

CONSTRAINTS:
- Pre-release; destructive schema changes are acceptable but no
  current need.
- Minimal comments, only the non-obvious "why".
- No AI/Claude self-references in code.
- The user runs + tests; you don't.

PROJECT MEMORY:
- `C:\Users\ben.talese\.claude\projects\C--Users-ben-talese-Downloads-DiscountDora-main-DiscountDora-main\memory\MEMORY.md`
  and every file it indexes. Read it before doing anything else.