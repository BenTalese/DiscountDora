# Doc Graph — (retired stub)

> **This map is retired.** It was a per-prompt required-reading cross-reference,
> but it went stale (FU-428: indexed only ~25 of 48 proposals and cited files that
> no longer exist) and was little used. It has been shrunk to this stub rather than
> maintained.

**What replaced it:**

- **Where things stand / what exists** → `PROJECT_STATE.md` (repo root) — the front
  door plus the full per-doc register.
- **Anti-drift before running a `03_prompts/` prompt** → assemble the required
  reading yourself (this is now a rule in `CLAUDE.md` / `AGENTS.md`):
  the governing proposal/impl-plan in `04_proposals/`, the relevant charter anchors
  + engineering rules in `01_charter/`, the feedback bullets it targets in
  `02_feedback/`, and any open follow-ups on that surface in `DORA_FOLLOWUPS.md`.

If a genuinely curated per-prompt reading map is ever wanted again, regenerate it
from the register rather than hand-maintaining this file.
