# Dashy Dora — Project State

**Regenerated: 2026-08-11 (hand-edited — **FU-609 (resolved):** finished the page-height contract sweep — converted the remaining 11 MainLayout pages to R-036 `<q-page>`. Doc-scroll (bare q-page) for Dashboard/Meal Plans/Templates/Price History/Cook Mode/Reports/Shop-Now/Stock detail; app-shell (`<q-page :style-fn>`) for the two runner shells + SettingsShell (dropping their hardcoded 100vh/`calc(100dvh-64px)` offsets); dual-host `<component :is>` for the peek-embedded stock detail. vue-tsc + eslint + compile clean on all 11; Dashboard + Reports live-verified as proper `<q-page>` (the other 9 couldn't be driven in the agent's non-composited browser — page FadeTransition freezes — so per-page walks are queued in DORA_VERIFY). R-036/ADR-032 marked fully converted. Prior same-day — **FU-618 (resolved):** restored the two pre-existing backend test breakages found during the FU-570/599 close-gate — a stale `infrastructure.llm.key_encryption` import in the bucket-C secret tests (module moved to `security.secret_encryption`) and two email-digest tests monkeypatching a now-gone `public_base_url` (the digest builds its link via `spa_deep_link` → `auth_helpers.public_base_url`, hash-history `/#/alerts`). Test-only fixes; **full backend suite now green: 1620 passed, 0 failed, 0 errors** (`pytest` no-args clean again). Prior same-day — **FU sweep #3 (FU-599 + FU-570 resolved):** two backend-integrity follow-ups. FU-599 — security actions (email-change, registration, password change, admin user CRUD) were double-logged in the audit trail; a request-scoped dedup flag now skips the middleware's generic row when a handler already emitted its own richer event (self-maintaining, post-commit-only so no coverage loss). FU-570 — a stale DB used to boot silently then 500 per-request; startup now logs a loud schema-drift banner (missing tables/columns vs the models, warn-not-refuse) and `/api/health` reports the DB's actual `schema_version_db` beside the code head. 3 new tests; audit/health/DTO e2e 43 green. Surfaced + logged FU-618 (pre-existing unrelated test red: bucket-C secret import rot + email-digest render). Prior same-day — **FU sweep #2 (FU-580 / FU-593 / FU-574 / FU-607 resolved):** four more follow-ups cleared. FU-580 — admin scanning + buy-verdict toggles now refresh their gated UI without a page reload. FU-593 — the suggestion feed reserves a slot per kind so a low-severity nudge (reconcile meals) can't be crowded out by noisy high-severity cards (new `_select_for_display` + 4 unit tests, backend suggestion suite 18 green). FU-574 — the "can't reach Dora's brain" error screen now offers a "Change instance URL" recovery hatch when a runtime backend override is set. FU-607 — dropped the stale stock-item image docstring (editing intentionally removed). vue-tsc + eslint clean; two owner walks queued in DORA_VERIFY (FU-580 admin session, FU-574 bad-URL). Prior same-day — **Opportunistic FU sweep (FU-598 / FU-614 / FU-616 resolved):** three self-contained follow-ups cleared. FU-598 — the Upcoming timeline's dots now carry a **shape** (circle/square/diamond) so expiry/shopping/meal stay distinct even on Pesto where two theme colours coincide (colour no longer load-bearing). FU-614 — the duplicated font-family option list + coalesce helper centralised into `themeService.ts` via a label-override factory (R-003). FU-616 — stale `router/index.ts` onboarding-escape comment tidied. vue-tsc + eslint clean; CHANGELOG (FU-598 only, user-visible); one timeline eyeball in DORA_VERIFY. Prior same-day — **Build — Meal Plans Part 2 cook batches COMPLETE (FU-617 resolved):** the "cook once, eat several days" feature is built end-to-end + verified live. Phase 5 (builder auto-proposal) landed on top of the backend (Phase 1) + manual UI (Phase 4): `build_week.py` now proposes cook batches for Batch households (one recipe per 3-day run per slot via `place_entries_batched`/`_batch_cook_span`; Fresh unchanged), the builder review shows "Cook once"/"Leftovers" markers with a defensive `validCookKey` (a review edit that breaks a group silently unlinks), and commit sends `cook_key`. Ties off FU-611. `test_build_week.py` +3 + `test_cook_batches.py` +1 e2e; consolidated backend gate **59 green**; vue-tsc + eslint clean. **Verified live:** "Build my week" rendered 18 Cook/Leftovers markers and committed **6 real CookBatch rows** (3 slots × 2 cooks / 6 days). Proposal Phases 2/3 stayed dropped (Σ-yield batch-invariance). CHANGELOG updated; FU-617 → RESOLVED; one light owner-walk (manual day-picker dialog) in DORA_VERIFY. Prior same-day — **Build — Meal Plans Part 2 cook batches: Phase 4 (manual UI) shipped + verified live (FU-617):** the cook-batch feature ("cook once, eat several days") is now usable end-to-end. On top of the tested backend (Phase 1), added the frontend: `MealPlanEntry` read fields + `cook_key` on the write command (mapped from `cook_batch_id` so links survive edits), `useMealPlanner.setCookDays`/`cookBatchDays`, per-entry "Cook · serves N" / "Leftovers" markers on the chip + rich card (gated on the Batch cook-style), and "Cook once for more days…" / "Separate this cook" menu → a day-picker. vue-tsc + eslint clean. **Verified live** (Batch on, 3×2 dinner batch): markers correct ("Cook · serves 6" on the earliest day + "Leftovers" ×2), menu shows both actions, "Separate this cook" dissolves the batch; no console errors. CHANGELOG entry added. **Only Phase 5 remains** — builder auto-proposal of cooks (Effortless enhancement; ties off FU-611). Prior same-day — **Build — Meal Plans Part 2 cook batches, BACKEND shipped (FU-617):** built + tested the backend for the cook-batch feature (one cook feeds several linked days). New first-class `CookBatch` entity/table + migration `d5a9f27c4e18` (single head, schema-match green), `MealPlanEntry.cook_batch_id`, `cook_key` grouping + structural validation on create/update (shared helper, R-003), forward-replace batch lifecycle, and a server-derived read view (`is_cook_day`/`cook_batch_total_servings`/`cook_batch_size`). `test_cook_batches.py` (8) + meal-plan router/preview/today suites green. **Scope correction from building-to-plan:** the proposal's Phase 2 (demand-collapse) + Phase 3 (reconcile) were dropped as **no-ops** — under the Σ-yield model the ingredient/cost/shortfall/pool math is already batch-invariant (pinned by an e2e invariance test); the earlier "6 double-count points" assumed a different serving model. Proposal §0/§6/§13 + FU-617 corrected. **Remaining is frontend only:** Phase 4 UI (per-entry linked markers + link/unlink menu, gated on the Batch cook-style) + Phase 5 builder auto-proposals (ties off FU-611). No CHANGELOG entry yet (not user-visible until the UI lands). Prior same-day — **Design brief — Meal Plans Part 2 (cook batches):** new `docs/04_proposals/PROPOSAL_MEAL_PLANS_PART_2.md` (design only, no code) after a multi-turn owner design conversation + a 4-agent read-only code investigation. Adds the batch-cooker's missing planning mechanic: link the same slot across several days to **one cook** ("cook X Monday, eat it Mon+Tue+Wed dinner"). First-class `CookBatch` entity (+ `MealPlanEntry.cook_batch_id`), servings split (batch = demand counted once at total yield, entry = per-day consumption), `cook_key` grouping on the write path (dodges the destroy/recreate identity problem), 6 aggregation impact points as a change-list, reconcile cook-once-drain-many, per-entry linked-cook UI (no layout rebuild — reuses the highlight-set plumbing), builder auto-proposals for Batch households (also the honest FU-611 fix). **Folds into the existing "Batch" cook-style** — no new setting/word. Coverage → F26 (the "essential" batch-vs-fresh feedback bullet). Open decisions all closed inline; build tracked as **FU-617**. Prior same-day — **FU-611 + FU-609:** **FU-611** — "Build my week" left later days blank when the day×slot grid outsizes the cookbook (the ranker only picks *distinct* recipes); added a client-only review-step hint (`showShortfallHint` in `MealPlanBuilderDialog`, keyed on the auto-build response's `days_used`×`slots_used` vs placed `entries` — no backend change), verified live on the 11-recipe seed (7×3=21 requested → 8 placed → 4 empty days). **FU-611 → RESOLVED.** **FU-609** — did the safe slice, not a blind 11-page sweep: **codified R-036/ADR-032** (a MainLayout route roots on `<q-page>`; doc-scroll → bare `<q-page>`, app-shell → `<q-page :style-fn>` reading the live offset; never hardcode the offset) and **converted `RecipesOverview`** (bare `<q-page>`) whose shared counts-footer floated mid-screen on short lists; **FU-609 stays OPEN** as the opportunistic per-page tail. Live browser walk confirmed the Cookbook root is now `<q-page>`, renders clean, no console errors; 2 visual eyeballs queued (0×0 pane). vue-tsc + eslint clean. Prior same-day — **FU-610 (D-019):** four meal-plan dialogs ("Save as a template" + "Apply recurring" in `MealPlansOverview`, "Save set" in `MealPlanTemplatesPage`, inline rename in `MealPlanTemplatesDrawer`) stopped binding their transient `saving*` flag to `:disable` on the input/select/date fields (D-019 — inerting a focusable field mid-save blurs it); Save buttons keep `:loading` + double-submit `:disable`. vue-tsc + eslint clean; focus-retention eyeball queued in DORA_VERIFY. FU-610 → RESOLVED. Prior same-day — **Meal-plan cleanup (FU-612 + FU-613):** two colocated findings from the 2026-08-07 builder rework, both closed. FU-613 — a phantom `--separator` CSS token (defined in no theme → invalid `border` declaration → borders drew as nothing) was swapped to `--border-default` in all 9 spots across `MealPlanEntryChip`/`MealPlanMobileFocus`/`MealPlanRichCard`/`MealPlanWeekDayCard`, so card outlines + dashed drag/drop affordances now render; visual eyeball queued in DORA_VERIFY. FU-612 — the per-user **`meals_per_week`** preference (orphaned when the day×slot toggle builder replaced the meal-count slider; zero readers) **deleted end-to-end** per owner call: FE composable `useMealsPerWeek.ts` + `BUILDER_TARGET_MEALS_FALLBACK` + the Settings→Preferences "Meal planning" section + FE/BE DTO fields + entity attr/`Fields` enum + table column, plus migration `c3f8a1b52d9e` (drops the `User` column, chained off head `b9d4f2a7c1e6`, single head, pre-release hard change). Verified: vue-tsc + eslint clean; 116 backend tests green; full alembic chain re-applied to a throwaway SQLite (column absent). Both FUs → RESOLVED. Prior 2026-08-10 — **Bug fix:** e-mailed password-reset / verify-email / confirm-email-change / alerts links dead-ended on the login screen — they were built path-based (`/reset-password?token=…`) but the SPA is hash-routed, so they resolved to `/` and bounced to `#/login`. Now built with the route in the fragment (`/#/reset-password?token=…`) via one shared `spa_deep_link` helper; verified live (link → "Choose a new password" screen with token applied), unit + e2e auth tests green. Prior same-day — FU-615: household **headcount + batch-cooking moved per-user → install-wide** (both dropped off `User` via migration `b9d4f2a7c1e6`, hard change; now on the `AppSetting` singleton, read by every client via new `/api/health.cooking_policy` + `useCookingPolicy`, edited in new **Settings → System → Cooking** admin page; removed from onboarding + the per-user Preferences "Cooking style" toggle). Backend e2e green (incl. new roundtrips), DTO snapshots refreshed (app_settings +2 / auth_me −2 / health +cooking_policy), full vue-tsc + eslint clean, single alembic head; **owner owes a visual walk of the new admin page** (pane wouldn't composite). Prior same-day — onboarding cleanup: slim top bar (mascot · progress rail · Skip-onboarding inline; sign-out + brand wordmark removed), wizard forced to a self-contained dark canvas (`data-theme="pesto-dark"`) so light-theme users don't see light-on-dark cards, step-1 trims ("Hi, I'm Dora.", theme option "System", display-name field removed — name is set at account creation), and "Restart onboarding" removed from Settings → About; owner questioned why cooking prefs (headcount + batch) are per-user — root cause is cook-mode convenience, relocation to an install-wide/admin AppSetting logged as **FU-615** (headcount left in onboarding until it has a new home). Verified live via DOM/computed-style; owner eyeball-feel checks in `DORA_VERIFY.md`. Prior same-day — stocktake fill-pulse + expiry menu wrap fixes; font picker names the default "Nunito (Default)"; recipe-card ingredient count; transactional email brand banner + password-reset copy cleanup; prior 2026-08-08 — Stock UI polish batch + Title-case page titles + `is_flagged`→`is_essential` rename. Essential flag renamed end-to-end (DB column via migration `f4a2c7e9b1d3`, entity, API, frontend — pre-release hard rename, no alias); mobile menu-bar + browser-tab titles now Title Case (D-008 carve-out); Stock Overview expiry dropdown icons + "Push expiry by N days" wording, Export button glyph, Stocktake glow now pulses the fill; Stock item detail — usual-store logos in the dropdown, Clear-expiry moved so clearing doesn't shift the buttons, recipe cards reflow by column count. Backend 299 tests + migration real-boot (column confirmed renamed) + frontend typecheck/40 unit tests all green; **visual pass owed** (preview browser dropped auth on reload + screenshots timed out) — see `DORA_VERIFY.md` 2026-08-08. Prior 2026-08-07 (hand-edited — Product Search config simplified: URL row back on Settings → Features, main-menu entry shows **only** when a URL is set (fixes a nav button that dead-ended normal users on an admin page); reverted the short-lived dedicated Products settings page + `product_search_hidden` toggle (migration `a1b7f4e9c3d5` deleted — **dev DBs need a drop_all reset**); verified live. Prior 2026-08-07 — meal-plan builder rebuilt around day × meal toggle rows (scope/count/slot-mode controls deleted; `auto-build` contract now `{days[], slot_names[], repeat_same_day}`), new shared `BaseToggleGroup` + style-guide **B2a**, plus **Duplicate to next week** (replace semantics) on the week headers; behaviour verified live, **visual pass owed** (preview browser measured 0×0 for the whole dialog); spun off FU-611/612/613. Needs a decision: **FU-612** — the `meals_per_week` preference no longer drives anything. Prior 2026-08-07 — settings-save focus-steal fixed across 20 settings pages (transient `saving` no longer disables focusable fields; new **D-019**); live browser pass still owed, see `DORA_VERIFY.md`; spun off FU-610. Prior 2026-07-31: **monetization reversed → donation / open-source, all features free**; FU-562 + FU-567 resolved won't-do, FU-406/557 reframed. Prior: **FU BACKLOG (verification paused; owner clearing FUs): FU-583 + FU-581 + FU-586 + FU-602/606/604/603 + FU-597 + FU-601 + FU-595**. FU-583 fixed (dashboard Dora Score "Freshness"/"Stocktake" actions linked to `/stock?expiring=1` / `?stocktake=1` but StockOverview ignored both params → landed on the full pantry; added an `expiringSoonOnly` filter + "Expiring soon" chip reusing the existing expiry helpers, mapped `expiring=1`→it and `stocktake=1`→the existing Needs-check chip; new Vitest cases, 31 green, vue-tsc + eslint clean). FU-581 fixed (several in-app "Product Search" entry points still pushed to the retired `/product-search` route → 404: stock-item Find-&-link CTA, My-Products empty-state + orphan-search, Dashboard "Hunt for deals", + two Dora quick-actions; all now route through one `openProductSearch` resolver — external companion when a URL is set, else the Features setup page. **Owner call:** the Product Search nav entry is now shown whenever products is on, even before the URL is set, to signal it can be set up — supersedes R-029 for that one entry). vue-tsc + eslint clean; owner live-walk (products-on × url-set/unset) queued. FU-586 fixed (dashboard money/deal cards stayed blank on a hard reload / PWA cold start — the card loaders gate on the once-per-document `/api/health` flags probe, which hadn't resolved by mount, and nothing re-ran them when it landed; `DashboardPage.vue` now `watch`es `moneyEnabled`/`productsEnabled` and re-fires exactly the gated loaders when a gate flips on; warm navs unaffected). No test — the referenced e2e specs were deleted in the smoke-only pivot; owner live-walk queued. vue-tsc + eslint clean. Prior same batch: Owner yes/no batch: FU-602 good_deal alert + deal-band control = deliberate descope (buy-verdict only, closed); FU-606 My-Products stock-aware bulk = won't-do (closed); FU-604 fixed (price-watch panel now gates on the per-user money opt-out too, via useMoneyEnabled); FU-603 fixed (cart tooltip no longer says "draft", picker defaults to a planning draft not the in-progress shop). vue-tsc + eslint clean. FU-597: the Alerts page rendered a stale feed (only refetched when the store was empty) — now always refetches on open, matching the header bell; vue-tsc + eslint clean. FU-601: settings save-failure toasts were grammatically broken across 6 pages ("Could not save <success sentence>..") — fixed + the duplicated save-toast helper extracted into one `useSettingsSave()` composable (R-003); vue-tsc + eslint clean. FU-595: past-unconsumed meal-plan entry no longer freezes/crashes the week (server preserves all past entries as history + client sends forward-only + failed writes toast instead of hitting the ErrorBoundary); new backend regression test; router+reconcile suites 36 green, vue-tsc clean. Prior same day: **VERIFY BATCH: History tab + FU-602 + Data pages + Help copy**: stock-detail History tab ("Opened" entry + Set/Pushed-expiry trail render); **FU-602 confirmed absent** (Settings → Notifications, money ON — no Deal-alerts section / Good&great control; `good_deal` alert + deal-band control don't exist in UI, buy-verdict half live — awaiting owner intent call); Data pages render (Import: Download-template + drop zone; Backup: New backup + drop zone + Restore — no regression, file flows stay device); Help "Add a stock item" copy matches the dialog (deleted). Agent-drivable pile thinning — remainder skews visual/device/viewport/fresh-install/synthetic-input. No bug, no code. Prior: **VERIFY BATCH: Add-item dialog + FU-316 + Stock Overview toolbar/expiry**: Add-a-stock-item dialog (Name/Level/Location/Stock-group/Essential, no Expiry; blank→"Name is required"; "  barilla pasta  "→"already exists"); **FU-316 quick-add driven end-to-end** (row cart→"Which list?" picker→pick "This week"→toast "Added to This week."→2nd add no-picker/session-remembered — section deleted); Stock Overview Chunk 2 (toolbar order, Bulk-select→Cancel, footer order) + Chunk 4 (no-expiry q-date picker, expiry menu +1/+7/+14/Clear no-+30). All verified + trimmed; added items cleaned up. No bug, no code. Prior: **VERIFY FU-165 SHOPPING-LIST UX v2**: rail+next-up marker, doughnut+totals, group-by (Location regroups), bulk-select, and Print all verified live; residuals (mobile dropdown, shop-day tones, finish-review flow, row actions, drag-reorder, dashboard-add) left as tight bullets. No bug, no code. Prior: **THREE CROSS-CUTTING VERIFY SWEEPS**: (1) **L160 dark-mode search-bar contrast** CLEAN — Cookbook/Stock/My-Products/Price-History search boxes + the Dora chat input all measured white text (lum 255) on dark backgrounds (lum 22–31, Δ≈220–233), no white-on-white. (2) **R-024 image-source-picker** — every image-upload surface routes through the shared `ImageSourcePicker`→`processImageFile` chokepoint (recipe hero + avatar via `ImageUploadField`; step images + store logo + receipts direct); verified live on the avatar (desktop shows just "Add (file)", camera hidden on `pointer:coarse=false`). Corrected 2 stale checklist items → **[[FU-607]]** (no stock-item image-edit surface exists any more, though `ImageUploadField`'s docstring still cites FU-126 adoption — confirm intent or drop the clause) and store-logo-is-now-on-the-shared-picker (FU-335, not a q-file carve-out). (3) **FU-334 receipts section-gating** — draft hides the Receipts section, shopping shows it (empty-state + desktop "Choose receipt", camera hidden), done keeps it (record-keeping survives finish); attachment CRUD/cascade backend-pinned. No bugs, no code; one finding (FU-607). Prior: **VERIFY BATCH 8 QUICK-ADD TOAST + "ALWAYS ASK" (FU-316)**: resolver backbone verified live (`POST /shopping-lists/primary/lines`, 2 seed drafts): no-hint → **`ambiguous`** + candidates `["This week","Big load list"]` (fires the picker), valid draft hint → **`added`**, repeat → **`already_on_list:true`** (drives the "Already on …" toast), done-list hint → **422 hint_invalid** (client re-prompts). Client wiring source-confirmed: named-list toasts (`Added to ${display_name}.`/`Already on …`), the `$q.dialog` radio picker with remember-for-tab vs always-ask copy variants, the `useQuickAddTargetPick` sessionStorage remember/clear (clear-on-always-ask so it re-prompts), the bulk-resolve-once path, and the Preferences "Always ask which list" toggle → PATCH `always_ask_which_shopping_list` → "Dora will always ask which list."/"Dora will remember your pick." Only the on-screen picker/toast eyeball is a light owner-walk (the stock-row cart affordance doesn't paint in the hidden pane). Resolver test data reverted; env clean; no bugs, no code. Prior: **VERIFY BATCH 8 TRIM-TO-BUDGET (FU-448)**: cleared the reachable UI on the money seed. Contrived a $20/week budget (spent $12.40 → headroom $7.60) + a draft list of 4 out-of-stock lines totalling $18 (prices via `actual_unit_price` on `PATCH …/lines/{id}`) → banner rendered **"Projected $18.00 · budget remaining $7.60 — trim $10.40 to fit."**; **Show what would be cut** → 0 cuttable (all 4 out lines showed BUY + were held by the **verdict=buy-on-out never-cut**) → **"Trimmed everything safe. Still $10.40 over…"** fallback; **Dismiss** hid the banner. The **cuttable → Keep → Trim-to-fit → Deferred → Add-back** happy path stays **owner-walk** with a precise reason: the running app can't create a cuttable line (every tier needs `added_via` auto-provenance [add-line API is `extra=forbid`/manual-only] or price-history buy-verdict; out items are always verdict=buy → never-cut; the deferred state is only written by trim-apply; the DB is Postgres so injection isn't clean) — the frontend chain wiring is source-confirmed and the classification/apply is backend-pinned. QA list deleted + budget cleared; env clean; no bugs, no code. Prior: **VERIFY BATCH 8 PUT-AWAY DIALOG (FU-452)**: cleared the finished-list Put-away surface on the money seed (money-agnostic). Drove the done "Last week" list (ticked lines across Pantry/Freezer/no-location): toolbar gating (Put-away present on `done`, absent on the draft "This week"), the "Put away" dialog's location-grouped cards (breadcrumb label + count chip, ticked-only), group-header tick → dim-collapse + green chip both ways, **(No location)** pinned-bottom/no-tick-affordance/per-item Assign, the Assign mini-dialog (Cancel = no mutation; the Save endpoint `PATCH /stock-items/{id}` `stock_location_id` → 204 moved the item into a new Fridge group + dropped the emptied (No location) group), and the ephemeral tick-reset on close/reopen. Two edge states (all-unsorted, zero-ticked banner) source-confirmed — seed done lists are fully-ticked and can't be un-ticked. No bugs, no code; stock-item location reverted. DORA_VERIFY FU-452 section deleted. Prior: **VERIFY BATCH 14 MY PRODUCTS + PRICE HISTORY**: cleared both money-gated product surfaces on the Windows money seed. My Products (FU-214/FU-208): mark-inactive → 204 no "Extra inputs not permitted" 422 (L193 fixed), linked chip green/inactive-dim/soft-delete-only semantics (L195/L198/L197), and **FU-208 "Link…" opens its dialog in place (no bounce) + `POST /stock-items/{id}/products` round-trips 204** (verified live: unlinked Freddo → linked → unlinked). Price History (FU-214 L218–L225): comparison card + money-formatted "Notify me below ($)" input, shared `positive` `-N%` chip, full-width chart, and a **theme-aware hover tooltip** (`--surface-component` dark `#162724` on light `#bfc8cf` in pesto-dark — no more white-on-white, L223 fixed). **Found + fixed a real production-visible bug — [[FU-605]]** (RESOLVED): picking products from the list never updated the chart/strip — `toggleSelect` mutated the `selectedIds` ref array in place under a **shallow** `watch`, so only the `?product_id=` deep-link (which reassigns) ever populated it; fixed by reassigning immutably, verified live (preselect 1 → add 2 → remove 1) after `quasar build -m spa`. **[[FU-606]] opened** (product decision): L205/L206 stock-level-aware bulk-select variants — recommend the generic "Select on-deal" suffices / won't-build (stock-aware buying already lives in Draft-my-shop / buy-verdict / auto-add). All DB mutations reverted; DORA_VERIFY FU-214/FU-208 sections deleted bar L160 (dark-mode search-bar sweep). Prior: **VERIFY BATCH 11 C-9.5 PRICE WATCH**: cleared the previously money-blocked price-watch tier end-to-end on the money seed — empty-state, arm→panel lists product·merchant·"notify below $X", View→price-history explorer deep-linked to the product, Remove→gone+toast, and hidden when the install money flag is off (took care to test the install flag, not just the per-user pref). Armed via the POST the "Set alert" button calls (the arm q-input didn't render in the hidden pane). One confirm-intent finding **FU-604** (the panel gates on install money while the trim-to-budget banner gates on the per-user opt-out — a split gate). All test data reverted, flags restored. No code changed. Prior: **VERIFY BATCH 9 (CART BUTTON) on the money seed**: first round using the FU-592 money-on seed (added a `dora-verify-backend-money-linux` launch entry). Cleared Cart Button **C2** fully (2+-products → QuickAddSheet as ONE surface with target dropdown + offer radios + qty; 0/1-product silent adds; the button is a toggle that removes an already-listed item; `linked_product_count` correct; the 2026-06-14 no-pop repro is gone), **C3-UI** product-only lines (`.shopping-line-product-only` tint + product chip, `product_id`/null), and **C4** the Axis-B generate picker (cancel → nothing; merge-into-draft; "+ Create new list" → "Meals: week of …"). Harness note: these Quasar controls need a full pointer-event sequence, not a bare `.click()`. One low-priority finding **FU-603** (cart tooltip says "draft list" but the picker targets any active list). All test data reverted. No code changed. Prior: **VERIFY BATCH 1 (TOP BLOCK) OPENED**: verified the **Dora bubble Basic-mode add-to-list (FU-429)** fully live — the DoraChat message box is a `v-model` textarea and IS synthetically drivable (unlike the one-way `:model-value` q-inputs), so all six paths were driven against real server list state (happy "add butter"→"Added Butter 🛒"+list; multi "garlic and olive oil"→both; not-found; ambiguous "cream"→not guessed; no-primary→prompt; bare→"what should I add?"), parser already Vitest-pinned; all drift reverted. **Currency & locale (FU-043)** core contract cleared: EUR/de-DE→"1.234,56 €" and reset→"$12.50" proven live. **Found + fixed a real bug — [[FU-600]]** (RESOLVED): the locale-page preview showed the AUD/en-AU defaults on cold load regardless of the saved policy — the page never triggered the `useMoney` load and, with money OFF (default), nothing else did; fixed by `refreshMoneyPolicy()` on mount, re-verified live after a `quasar build -m spa`. Also removed dead retailer-name-drop copy (the retired `/product-search` page-help in `doraIntents.ts` still said "Coles, Woolies, IGA, Aldi…" — FU-043 item 8). **Stores logo (FU-335)** structure source-confirmed (dialog body won't paint in the hidden pane; interactions device/owner-walk); **onboarding story** is V-pack owner-walk. **Follow-on: cleared 3 more Dora-bubble FU-360 leftovers live** — Hide-Dora (toggle+toast+reload-persist), mode-slider disabled/no-LLM state, text-size rem-scaling (md→xl proportional); **found [[FU-601]]** (settings save-failure toasts grammatically broken — "Could not save dora helper shown.." — copied across 6 settings pages, R-003 duplication smell, error-path/cosmetic). Batch 1 → 🟡. **Then opened the last fully-untouched batch — VERIFY BATCH 16 (CROSS-CUTTING B)** (no code changes, read-only): P8-01 rename (zero "Discount Dora" in FE+BE source/DOM; tab titles/mascot/About/version-replies confirmed), D.O.R.A. bot rename, route retirements (`/data/barcodes`→qr-labels; `/data/export`+`/data`→admin-data-backup — checklist's "→404" is stale, superseded by FU-341), health flags match the Features panel, **R-016 lazy hydration** (XHR-instrumented: stock-items/recipe-list/vocab/meal-slot all fire once, 0 on revisit), feature-flag panel (7 toggles match `/api/health`), formatQuantity unit-pinned, unsaved-guard observed live prior. **Both previously-untouched batches (1 + 16) are now opened** — the verify campaign has touched every batch. **2nd pass same day: built + RESOLVED [[FU-592]]** (the `DORA_SEED_MONEY_ON` seed knob — env-gated, default-off, backend suite green) and used it to **clear the money/nutrition-gated UI live**: recipe **cost card "$6.96 (3/4 priced)"**, **kcal input**, **nutrition card**, cookbook **Kcal sort + filter**, dashboard **budget card** — this unblocks the money-gated verify items across Batches 5/7/9/11/16. Also verified **DnD-affordance class parity** (shopping lines + recipe ingredients share `.dora-dnd-row`/`.dora-dnd-handle`; old per-surface classes gone), **header peer buttons** (Help/avatar nav, no chevron), and **settings-shell window-no-scroll**. Remaining Batch-16 owner-walk: mobile-header viewport, menu-indicator colour, ring animations, reduced-motion, the drag interaction itself. **Then ran the money-gated sweep** (money-on backend live): **dashboard budget-target** (FU-297, tracking body), **budget-defense swaps** (FU-451 — full flow: panel → Preview dialog → Apply/Undo → dashboard bullet), **trim-to-budget banner** + "everything safe" fallback + never-cut set (FU-448; cuttable→Deferred happy path needs a safe-to-cut list → owner-walk), **buy-verdict** live (BUY badges). **Finding [[FU-602]]:** FU-450's `good_deal` alert kind + "Good & great/Great only" deal-band control are absent (the deal-quality band feeds buy-verdict only) — likely a deliberate descope, unconfirmed. **Batch 7 templates (later):** save/apply/manage verified live (dialog, Save→toast+server-created, Apply→"Added 6 meals" fork, `/meal-plans/templates` rotating-sets manager). **DORA_VERIFY convention correction:** stopped replacing verified lines with evidence essays — delete-on-pass restored, evidence lives in worklog/triage; file trimmed ~1714→~1660 despite the session's work. Prior: **VERIFY BATCH 13 (SETTINGS B) OPENED**: cleared the account + **CSRF defence** block (cookie flags, `PATCH /auth/me {email}` → 400, the CSRF gate proved in BOTH directions from a cold jar — no header 403 / with header 200 / public login exempt 200, every SPA mutation carrying a header that matches the cookie, the email-change enable-gate and both password paths, and both audit events with correct severities) plus the assistant API contracts (no plaintext key leak, cross-field 422, health tracks the master switch, hygiene grep clean, kill-switch driven both ways) and both FU-285 copy items. **Finding FU-599:** handlers with an explicit `audit_emit` ALSO get an auto-audit row, so account-security actions are logged twice under two naming conventions — `_NO_AUDIT_ENDPOINTS` only covers login/logout, leaving ~10 emitters double-logging. Left unfixed on purpose: suppressing a generic row whose explicit counterpart is thinner would silently LOSE audit coverage. No code changed this round. Prior: **VERIFY BATCH 11 (ALERTS) OPENED**: cleared most of the C-9.1/9.2/9.3/9.4/9.6 spine — badge==actionable count, snooze persists + correctly kind-scoped, dismiss hides on page/peek/dashboard, threshold round-trip 7→2→7 reshaping the feed, disable-a-kind and demote/promote with exact tier accounting, hub + slim peek + History-with-names, BOTH C-9.4 nudges end-to-end (show → deep-link → clear), the Upcoming timeline (dots, dimming, today ring, click-expand, all 3 link targets), the reconcile-overdue row, and the SMTP/VAPID off-gating. **Fixed:** the hub's History labelled `out_of_stock` as "out of_stock" — a hand-rolled `replace('_',' ')` only swaps the first underscore — now reads the shared kind meta via a new `kindLabel`; 3 tests added (411 green). **2 findings:** **FU-597** (the Alerts page only refetches when its store is empty, so it can show a stale feed all session — caught it still nagging "plan some meals" after a meal was planned) and **FU-598** (`--brand-primary` and `--semantic-positive` are the same hex in Pesto, the default theme, so the timeline's shopping and meal dots are indistinguishable). Prior: **VERIFY BATCH 7 BIG ROUND #2 (C-2 planner walk)**: cleared meals-per-week 5/5 (section deleted), Print 3/4, useListState remainder 3/4, and most of the C-2/R-Phase walk (tap-add slot correctness — F35 holds, ± stepper live-adjust + remove-at-0, past-day inertness, implicit create, Clear-week, the full calendar incl. `?monday=` across reload, sidebar list-status rows, and the 3-step builder end-to-end). **Found FU-595 — production severity, logged not fixed:** a past-dated entry with no `consumed_at` makes every add to that week 400 and hard-crash the planner to the ErrorBoundary; the server preserves history by `consumed_at` but validates by date, so such an entry is unrepresentable. Reachable via auto-drain-OFF and via reconcile → "Didn't cook". **Fixed:** the meal-plan print view titled every nameless planner week literally "None" (the planner makes nameless plans by design) → now "Week of <date>", pinned by a new backend test — my first version of that test was **vacuous** (seed-conditional, passed with the fix reverted) and was rewritten to create the nameless plan itself. **FU-596** opened: the builder files every meal under Breakfast. Prior: **VERIFY BATCH 7 (Meal plans) BIG ROUND**: first verify session driven from the Linux checkout (added `dora-verify-backend-linux` to `.claude/launch.json`; the existing entry hard-codes the Windows venv path) after rebuilding a 4-day-stale `dist/spa`. **Found + fixed a real 404**: the reconcile recap's **Done** button pointed at `/dashboard`, which is not a route (the dashboard is `/`), so the last tap of the flow landed on the "page wandered off" screen — the only such navigation in the SPA; fixed, rebuilt, re-walked green, vue-tsc clean. **Meal reconcile 14/15 walked** (all five verbs + both auto-drain modes with pool math verified against the DB at every step, threshold alert+suggestion, empty state, help dialog, XL type-scale, 3 theme families, admin settings page); survivor = the non-admin banner. Also cleared **Templates drawer 7/7** (section deleted), Show-all-slots 5/6 (incl. driving the storage-disabled path for real), useListState 4/8, C-2.I trays partial. **2 findings opened, deliberately not fixed** — **FU-594** (reconcile `skip` reverses the pool drain, contradicting the module's own docstring: pool 10→14 on a planned-4 entry; which side is wrong is a domain call) and **FU-593** (the `reconcile_meals_pending` suggestion is severity-low and gets crowded out of the hard 8-item feed by `use_soon` noise). **Browser-driving recipe v2** recorded in the triage banner: reloads ARE safe and unlock every persistence check, `$q.screen` freezes at boot width so resize must precede the reload, screenshots time out in this pane, page-driven mutations need the CSRF header echoed, and never rebuild the SPA mid-walk (swapped chunk hashes make the router silently keep the old view — cost ~20 min and a briefly mis-called navigation bug). Budget-defense swaps remain blocked on FU-592. Prior: **BIG ROUND #2: money/nutrition ON-state attempted → harness wall → FU-592**: the money/nutrition-gated surfaces (Chunk 9 cost/kcal, buy-verdict, budget) gate on BOTH install flags AND per-user prefs, all read once at cold mount; PATCHing them on mid-session doesn't re-render (stores stale), a reload re-sticks the rAF splash, and the login-form re-auth is blocked by the q-input synthetic-injection limit — so there's no in-session authed+shimmed+flags-on pane. Spawned **FU-592** (a `DORA_SEED_MONEY_ON` seed knob to boot money+nutrition on) as the clean fix; deliberately not rushed as seed/startup code at turn's end. Reverted all DB drift via a fresh re-seeding `preview_start` (flags confirmed default-off); re-confirmed item 201 off-state. Prior same day: **BIG ROUND: recipe versions + MealStepper + cost/nutrition off-state**: (A) kebab New-version made "Veggie Stir Fry (v2)" with no 500 (live FU-590 confirmation), sharing a back-filled version group + 1 sibling, inheriting ingredients/cuisine/servings, un-favourited, "Other versions" card on both copy & source, delete→card-disappears — Chunk 8 ➗; (B) MealStepper ± live-adjusts the pool 3→0 with "Remove one meal" disabled at 0 + server-persist — Chunk 3 🟡; (C) with money+nutrition flags OFF the detail has no kcal/cost/nutrition surfaces & the overview no Kcal sort/filter — Chunk 9 item 201 ✓, but flags-ON is HARNESS-BLOCKED (flags read once at cold mount; reload re-sticks the splash) → owner-walk. Banked: flag-ON UI needs a seed booted with the flag on (a launch.json env var would unblock it). Prior same day: **Recipe importer paste flow + bulk-linker walked live (two chained rounds)**: Import dialog → pasted a synthetic recipe → parsed to name/servings/source/3-steps/5-ingredients with the fuzzy matcher linking "olive oil"→Olive Oil and 4 free-text rows preserving raw_text (cookable=null neutral); then the bulk-linker page (`/settings/admin/data/unlinked-ingredients`) showed each as "raw_text · Used in 1 recipe" (FU-588 count-0 bug confirmed gone), and Create-new created a stock item = raw_text + linked it + dropped the recipe's unlinked count. Cleaned up (count back to 11). Both importer registers ⚪→➗; owner-walk left = the Link autocomplete (q-select not synthetic-drivable, endpoint pinned), empty-state, multi-site corpus, Android share. Prior same day: **RecipeEditDialog stub-creator (FU-095) walked live**: New-recipe modal shows exactly 4 fields (Name/Cuisine/Category/Collection, no heavy fields), "Create & open" button, Name→navigates to `/cookbook/<new-id>`, Cancel creates nothing (count 11→11); edit-from-overview is N/A by design (cards expose only ♥/chef-hat/add-to-list — no edit action anywhere on the overview, deep edit is on the detail page). Section → ✅. Prior same day: **Cookbook detail edit-mode + personal-notes cook-mode card walked live**: verified the Personal notes field placement (below Source URL, distinct textarea) + its cook-mode "Your notes" card with preserved line breaks (FU-432 done → ✅) + no-card-when-noteless; the Structured/Freeform/Image mode toggle (each renders own editor) + freeform-payload non-destructive flip; and the detail Tools multiselect populate. Banked: the detail page has an unsaved-changes route guard and `…/cook` nests under the detail route, so a dirty detail form blocks the cook nav (discard or hop to `#/` first). Personal notes register ⚪→✅, image-steps ⚪→🟡. Prior same day: **Cook mode Chunk 5 structured visuals + Chunk 10 sections finished**: no seed recipe had structured steps, so contrived a cookable structured recipe via the API (`steps_mode:structured` + sub-step + hints + step-referenced ingredients/tools + 2 named sections; raw fetch needs the `dora_csrf`→`X-CSRF-Token` echo) and verified in cook mode — step-referenced ingredient tint + 3px left accent, referenced-tool-lights vs unreferenced-tool-dims, "Sub-step" chip + All-steps indent, hint line + lightbulb, and section chip + section-grouped ingredient/step panels; deleted the recipe after (204). Chunk 5 register 🟡→➗. Prior same day: **Cook mode surface (Chunks 1–3 / 5 / 6) walked live + delete-on-pass**: drove Veggie Stir Fry cook mode and verified location-grouped ingredients, no mid-cook stock-level chip / no checkboxes, quantity spacing (`300g` vs `2 cloves`), the Sous Chef (?) commands popover (with "Done" gone), the auto-detected step timer (02:00 countdown + fill-bar + PAUSE/Reset), the unstructured-fallback ingredient highlight, "All steps" jump-to-index, the tools panel, and "Cooking for" defaulting to servings. Registers: Cook Mode Chunks 1–3 ⚪→➗, Chunk 5 ⚪→🟡, Chunk 6 ⚪→➗. Survivors = structured-recipe visuals (need a structured seed recipe), timer-expiry colour/beep, themes, finish flow (FU-591), Chunk-6 clamp (code-correct, not synthetically drivable). Prior same day: **Cookbook DETAIL page (Chunk 4 / FU-089) walked live + delete-on-pass**: drove the recipe-detail page on the seeded backend and verified the sticky toolbar (Mark cooked / Cook mode / Log cook… / Print / Save + kebab New-version·Delete), Mark-cooked pool-bump + last-cooked stamp, blank-name save-block ("Give the recipe a name."), single Missing-wins chip + red `.miss` tint on missing rows, the not-cookable cook-mode guard + working Cancel, and Cook-mode Exit→detail. DORA_VERIFY Chunk-4 trimmed (verified-note + 4 bullets removed); TRIAGE register row ⚪→➗. Survivors are layout/dialog-flow owner-walks. Next: the Cookbook edit-dialog + step-mode sub-surfaces and cook-mode Chunks 1–6. Prior same day: **agent browser-verify DIAGNOSED + UNBLOCKED**: the pane half-mounted (cards/dialogs never painted) because it runs the doc **hidden** → browser suspends `requestAnimationFrame` → the app's rAF-gated splash-dismiss + Vue transitions never complete. **Fixed with a recipe** (verified: 11 Cookbook cards + groups + chips paint): shim rAF→setTimeout, log in **in-place** (native input events, no reload → shim survives), navigate only via `location.hash` (never a full reload). So agent UI verification of card/grid/dialog/flow content IS viable now — recipe in DORA_VERIFY_TRIAGE banner. Verified earlier: FU-500 nav-hide (deleted); support-channel /health (API); Cookbook overview surface. Prior, **STANCE CHANGE (owner): verification is now LEAN + manual-first; Playwright descoped to smoke-only.** The "codify every regression-worthy check as a test" ambition was reversed as foolish for a solo-maintained, manually-UAT'd app. Playwright leaned to `auth.setup`+`login`+`smoke` (9 tests — proves the built SPA boots/routes/authenticates, the one thing Vitest+pytest can't); the ~22 flaky feature-behaviour specs + drive.mjs + cook-mode-finish.wip deleted. **Verify by driving the app once (agent or owner) and deleting the line; add an automated test only for stable low-churn contracts (backend/Vitest).** Backend/Vitest tests already written are kept (fast, reliable, caught FU-588/589/590). Stance codified in CLAUDE.md + DORA_VERIFY_TRIAGE top banner. **FU-591 (e2e full-suite instability) RESOLVED by descoping.** `DORA_LOG_LEVEL` ops knob kept. Prior same-day e2e-pivot work (cook-mode finish flow attempt, FU-591 diagnosis) is now moot under the smoke-only scope. Prior, **Batch 15 (Cross-cutting): security response headers (FU-459) codified clean** — the harness runs on this box (chromium-1228 + venv backend on :5171; smoke+login 9-pass gate) and `cook-mode-finish` drives the real loop (Finish → Down-one/Out/Unchanged → server-verified level drops + meals toast + Cancel-fires-nothing), passing 4/4 in a fresh env — but it's FLAKY on repeat (cook-mode loads the stock store async; a fast Finish click races it → decrement no-ops) AND **the full e2e suite doesn't run green in one long single-worker pass (14 failures from env degradation on ~13 individually-green specs → FU-591)**. Deactivated the spec to `cook-mode-finish.wip.ts` (driver blueprint), reverted the DORA_VERIFY bullets to owner-walk. **Takeaway: the e2e infra must be stabilised (parallelism/sharding + quiet the debug backend) before it's a reliable UI-codification vehicle; the reliable progress remains the backend-slice codification.** Prior same day, **Batch 15 (Cross-cutting): security response headers (FU-459) codified clean** (no bugs) — new `test_security_headers.py` (3 tests): all four headers (nosniff / X-Frame-Options DENY / Referrer-Policy / CSP) land on a 200, a 404, and a domain-error response; was completely untested. Full backend **1568 passed**. The campaign's server-ownable slices are now largely exhausted across non-parallel surfaces; remaining is UI/component + device-pack + owner-walk. Prior same day, **Batch 14 (Products): sub-slices delete-on-passed + stale bullet fixed** (no new tests) — PreferredBuy CRUD/sort/scope + cascade (FU-211) and price-observation log→unit_cost/remove (FU-213) confirmed already backend-pinned (green in the 1565-pass run); **corrected a stale DORA_VERIFY bullet** — FU-211 "reorder (up-down)" was retired by FU-225 (SPA sorts alphabetically now). Prior same day, **Batch 12 (Settings): Users admin add/delete behaviour (FU-461) codified clean** (no bugs) — new `test_users_admin.py` (7 tests): create→200+one-time-pw+listed, taken-username/email + malformed-email→422, **self-delete→403**, delete-another→204, unknown→404. SMTP/VAPID secret-never-leaks + admin-gate + PATCH last-admin guards confirmed already backend-tested. Full backend **1565 passed**. Prior same day, **Batch 8 (Shopping lists): `display_name` self-labelling (FU-165) codified clean** (no bugs) — `test_shopping_list_planned_shop_date.py` +1 test: the server-owned name→planned-date→re-label→created-fallback ladder (R-003), asserted against the imported `format_list_date`. Receipts/has_substitutes/trim-to-budget confirmed already backend-tested. Full backend **1558 passed**. Prior same day, **Batch 9 (Shopping lists): Cart Button Chunk 3 product-line rules 1–3 (FU-132) codified clean** (no bugs) — new `test_shopping_list_product_lines.py` (5 tests): product-only line create + no-anchor→422 (checklist guessed 400), Rule-2 orphan upgrade (convert-in-place + fold), Rule-3 delete cascade to nested product line. `has_substitutes`/trim-to-budget/`linked_product_count` confirmed already backend-tested. Full backend **1557 passed**. Prior same day, **Batch 5 (Cookbook B): recipe cost math + step-text validation codified clean** (no bugs) — `test_recipe_router.py` +5 tests: `estimated_cost = Σ qty × offer.price_now/size_value` (2dp) over the real StockItemProduct→Product→ProductOffer join [guards the FU-463 SQLite-uuid regression], partial-coverage priced/total counts, None-when-unpriced; whitespace-step→400 / empty-step→422. Full backend **1552 passed**. Batch-5 remainder now pure UI. Prior same day, **Batch 5 (Cookbook B): recipe versions codified → 2 REAL BUGS fixed** — new `test_new_recipe_version_numbering.py` (4 tests) caught **FU-590** (production-severity: `POST /recipes/<id>/new-version` **500'd for any recipe with ingredients** — autoflush of unparented ingredient clones + an R-032 noload clone that silently unlinked linked ingredients → anchor CHECK fail; only ingredient-less stubs versioned, which is why the lone existing test missed it) and **FU-589** (numbering skipped v2: v1, v3, v4… from a pre-flush sibling count). Both fixed (no_autoflush + clone from loaded FK; flush the group back-fill before counting). Full backend **1547 passed**. Prior same day, **Batch 4 (Cookbook): bulk-linker Link endpoint codified → REAL BUG fixed (FU-588)** — writing the first e2e test showed `GET /recipes/unlinked-ingredients` returning every group with `count: 0` / empty recipe list: the handler read the recipe FK off the un-prefixed `recipe_id` but the ORM binds `_recipe_id` (R-032, same family as `_stock_item_id`), so the bulk-linker page showed **"Used in 0 recipes"** for every ingredient (cosmetic — Link matches by text, still worked). Fixed + new `test_unlinked_ingredients_bulk_link.py` (4 tests) + the false-confidence unit stub corrected. The bug hid because the endpoint had no e2e test and the unit stub used the wrong attribute name. Full backend **1543 passed**. Prior same day, **Batch 4 (Cookbook) opened**: the server-ownable slices were mostly already pinned (cookability+expiring filters, tri-state cookability, import routes, bulk-linker grouping) → confirmed green + delete-on-passed; **2 gap-fills added** — `create_recipe__NameOnly__CreatesFreeformEmptyStub` (FU-095 stub defaults) + `taste_video_carousel_cruft_stripped` (importer L148: the loose corpus test can't catch Coles/video cruft creeping back). Full backend **1539 passed**. Batch-4 codify-next: the Link/Create-new bulk endpoints + ingredient DnD (needs a harness). Also **Batch 6 (Cook mode) opened**: rescale (FU-101) + personal-notes (FU-432) codified. `scaleQuantity.spec.ts` gained a Chunk-6 checklist describe block (+6 → 23 tests: up/down 1.5×/0.5×, ¾/1½ snap, egg 0.75→1 floor, 7.5g→7½ gram edge) and `test_recipe_notes.py` gained new-version note carry-over (+1 test); full Vitest **408/32**, touched backend file green. DORA_VERIFY pure-math + L305 bullets deleted, survivors reworded to component-clamp/UI-render owner-walks; L333 default-input reconciled (input seeds from onboarding `household_headcount` then `servings` — seed user has none, so checklist holds). Batch-6 codify-next (finish-flow dialog, voice/timer, Chunk-5 highlight) needs a cook-mode e2e/component harness that doesn't exist yet. Prior, Batch-7 verify: **FU-505 unlinked-warning codified → REAL BUG fixed (FU-587)** — the recipe + meal-plan sources of auto-generate ("Generate shopping list for this week" + Draft-my-shop's meal-plan slice) read `ingredient.stock_item` off a `lazy="noload"` relationship that was never included (R-032), so they **added zero linked recipe ingredients** AND **mis-reported every ingredient as unlinked** in the FU-505 "Add these manually" dialog; fixed to read the loaded `_stock_item_id` FK in both collectors + new `test_auto_generate_unlinked.py` (3 tests) regression pin; full backend suite **1533 passed**; CHANGELOG + FU-587 logged. Also codified FU-181 meals-per-week server bounds guard (0/-3/22/100/3.7 → 400, edges 1/21 ok). Parallel verify session, later 5: **Cards-menu reorder (FU-294) + `dashboard_layout` (FU-292) codified** — new `dashboard-cards-reorder.spec.ts` (tap arrows with first/last disabled states, native HTML5 drag-handle drop-on reorder, cross-zone rejection with server layout untouched, reload + second-context "another device" persistence, iPhone-UA handle-hidden run; layout captured/restored around the spec). FU-292 section deleted (migration + backend test long green, walk automated); FU-294 down to one mid-drag visuals bullet. Full e2e **84 passed / 0 failed**. Earlier, later 4: **price-drops (FU-296) codified → REAL BUG fixed** — the second-ever product price update in an install 500'd (`update_product.py` archived the outgoing offer without `repo.add()`, so the cascaded insert carried `EMPTY_UUID` and collided on the PK; ingest + seed paths were fine). One-line fix + CHANGELOG; found by new backend pins (ranking %-desc/amount tie-break, inactive exclusion, limit clamp 1–20 default 5 — `test_reports_router.py` +4, backend **1528 passed**) + new `dashboard-price-drops.spec.ts` (defaultHidden → menu enable → honest empty state on seed truth; engineered $10→$8 drop renders store/deep-link/"was"/"20% off"; deactivate → empty; toggle restored; warm-navigates around the FU-586 flags race, which also hits the products-gated loaders — addendum logged). Full e2e **80 passed / 0 failed**. FU-296 verify section down to 3 walk bullets. Earlier, later 3: **donut deep-links (FU-299) + "Next to cook" (FU-298) codified** — new `dashboard-donut.spec.ts` (legend/action hrefs vs server truth, inert card + green arc, two link arcs with aria-labels, a real pointer click on the SVG low arc landing the level-filtered stock view, Enter/Space keyboard nav) + `dashboard-next-cook.spec.ts` (throwaway today-Breakfast trio owns the card top-3: dedupe/order/meta + Ready·Missing 1·No ingredients badges vs DTO truth, name/Cook deep-links). FU-299 verify section deleted, FU-298 down to the empty-state bullet. **FU-584 addendum:** the hash-router wedge also fires on warm goBack→push (deterministic; both specs route around it). Full e2e **78 passed / 0 failed**. Earlier, later 2: **Log-price quick action (FU-300) + budget money-gate (FU-297) codified** — new `web_app/e2e/dashboard-log-price.spec.ts` (4 tests; flips both money layers on for the run and restores the seed's money-OFF state): three-button quick-action bar, LogPriceSheet picker (low/out-first shortlist vs server truth, live filter, title + detail-DTO prefill), submit happy path ($6.40/2 L → toast + folded-shape observation + Your-Prices "$3.20 / L" render, self-cleaning), dismiss-means-cancel + fresh reopen, and money OFF hiding Log price + the budget card (dashboard + Cards menu, zero budget-status requests). Found **FU-585** (back arrow keeps the search query vs checklist expecting cleared — design call, behaviour pinned) + **FU-586** (dashboard money loaders race the one-shot /api/health flags probe on cold mount and never re-run — money cards can silently skip on a hard reload; recommended fix "now"). Full e2e **73 passed / 0 failed**. DORA_VERIFY FU-300 section deleted; FU-297 down to one FU-586-blocked bullet. Prior same day: **Dora Score / Kitchen health (P8-08) codified** — new `web_app/e2e/dora-score.spec.ts` (3 tests: hero + ordered component rows vs `/dashboard/dora-score` server truth, the dormant-Budget contract [—, no bar, composite unchanged], action links incl. the deliberate no-Waste-link, Cards-menu toggle) + the L923 log-line contract added to `test_dashboard_router.py`; backend **1525 passed**, dora-score spec 4/4. Found **FU-583** (`?expiring=1`/`?stocktake=1` links ignored by StockOverview) + **FU-584** (full-suite detail-page specs flake on hash-goto race; also: e2e needs a fresh `dist/spa` build, and this box's node_modules was missing workbox deps that hard-block `quasar build` until `npm install`). DORA_VERIFY P8-08 trimmed, Batch-10 progressing. Prior 2026-07-18 design-audit session: **design audit ACTIONED** — new authoritative `docs/01_charter/DESIGN_STYLE_GUIDE.md` (D-001..D-015: level-colour semantics, contrast floors, type/tap minimums, icon+metaphor rules, single date/number authority, skeleton loading, dialog/toast conventions, motion usage, page composition, data-density, state legends, copy voice, pattern reuse) enforced on every UI task via new **R-035/ADR-031** in ENGINEERING_STANDARDS + CLAUDE.md; actionable backlog `docs/04_proposals/DESIGN_REMEDIATION_PLAN.md` (DR-1..DR-16 in 4 waves with full FU-578 coverage table; 🔴 owner sign-offs needed: DR-6 finish-pickers build-vs-cut [FU-582, renumbered from a FU-580 collision], DR-10 nav labels, DR-16 onboarding activation step). Synthesis rationale: `docs/05_investigations/UX_DESIGN_CRITIQUE_2026-07-18.md`. Parallel verify-campaign session same day, later 15:  **verify Batch 3 flipped ➗; Batch 10 (Dashboard) opened with Draft-my-shop codified** — new backend `test_auto_generate_draft_shop.py` (zero candidates → `nothing_to_add` + NO phantom list; explicit "Weekly shop · <date>" name honoured) + new `web_app/e2e/dashboard-draft-shop.spec.ts` (one-click happy path: toast → navigate → named draft with `auto:` provenance chips; Cards-menu toggle hides/restores), with an afterAll net preserving the sole-draft world. Suites: e2e **65 pass / 1 FU-576 flake**, backend **1524 passed**. Batch 3's remainder is owner-walk visuals only. Earlier same day, later 14: **verify Batch 3 — C-1b.3 Products tab codified, FU-581 found** — new `web_app/e2e/detail-products-tab.spec.ts` (2 tests: exactly-one Cheapest highlight + "Link another" on Milk's linked products, no retired "Get cheapest"; empty-state "Find & link a product" CTA on a throwaway item). **Real bug FU-581:** the CTA — plus two My Products links and the Dashboard "Hunt for deals →" CTA — still routes to the FU-186-retired `/product-search` and lands on the 404; needs an owner call on the per-surface target (external `product_search_url` vs My Products vs hide), so logged rather than blind-patched. Full e2e **63 pass / 1 FU-576 flake**. Batch 3 is now fully triaged — remainder is visual/viewport/install-gated walks only. Earlier same day, later 13: **verify Batch 3 — C-1b.4 tab actions codified** — new `web_app/e2e/detail-recipes-tab.spec.ts` (3 tests on a throwaway API-created universe: favourite heart toggles both ways with server truth; cookable "Add all to list" lands every ingredient on the draft + Lists tab reflects it; substitutes row has no retired "Swap into list" and Remove unlinks). Full e2e **61 pass / 1 FU-576 flake**. C-1b codify-next remaining: only the Products-tab gating (C-1b.3). Earlier same day, later 12: **verify Batch 3 — C-1b triaged, Chunks 2/4 codified** — the ~28-bullet C-1b detail section rewritten against current coverage (pinned + stale bullets deleted incl. the FU-511-retired auto-add mentions; compressed to visual walks + 2 codify-next candidates: C-1b.4 Recipes-tab actions, C-1b.3 Products-tab gating); new `useStockItemActionsPushExpiry.spec.ts` (8 Vitest tests — the FU-123 `max(today, current)+N` expiry-push matrix) + a Chunk-2 `stock.spec.ts` describe (desktop filter-panel persistence reload round-trip; retired "Used in a recipe" filter guard + bare "Search" placeholder). Suites: e2e **58 pass / 1 FU-576 flake**, Vitest **402/32**. Batch-3 tail = visual/viewport walks + Offers-sidecar line + the 2 codify-next candidates. Earlier same day, later 11: **verify Batch 3 — Stock pickers behavioural halves codified** — new `web_app/e2e/stock-pickers.spec.ts` (2 tests: detail Level row round-trip on a throwaway item → "Updated just now" + server truth; overview level-filter set→clear cycle console-clean with the "Any level" fallback trigger); full e2e **56 pass / 1 pre-existing FU-576 flake**; pickers section down to one dot-styling visual; Batch-3 tail is now just the C-1b detail section triage. Earlier same day, later 10: **verify Batch 3 — FU-109 deep-link filter codified, 2 real bugs found + fixed** — new `web_app/e2e/recipe-deeplink.spec.ts` (4 tests: chip land/narrow/teardown, Clear strips `?recipe=`, bogus-id degrades silently, icon only on the detail Recipes tab + no-dim). The spec caught two real defects, both fixed inline: a dead `?recipe=` id stranded a phantom "Ingredients of: this recipe" chip (recipe store's hydration latch made reactive, chip gated on resolved-or-loading), and the Stock Overview empty-state "Clear" left `?recipe=` in the URL for a refresh to reinstate (rewired to `clearAllFilters`). Also fixed FU-579's bulk-waste half — its 12 type errors were hard-failing `npx quasar build` and thus the whole e2e pipeline from a clean tree; `vue-tsc --noEmit` now fully clean. Full e2e **54 pass / 1 pre-existing FU-576 flake**; Vitest 394/31. Both FU-109 DORA_VERIFY sections deleted. Earlier same day, later 9: **verify Batch 3 — History-tab sections fully codified** — new backend `tests/e2e/dora_api/test_stock_item_history_feeds.py` (7 tests: the Bought feed's ticked+finished-only contract with optional price/store, and the Cooked feed's RecipeIngredient-join-only + meals-badge + zero-meal-silent contracts; backend suite **1522 passed**) + new `web_app/e2e/history-tab.spec.ts` (4 tests: Sriracha "16 older events not shown" cap footer stable across reload, Milk mixed-kind feed with no footer, "Used in <recipe> · N meals" badge, full Set→Pushed→Cleared expiry-trail render on a self-deleting item; e2e suite 50 pass / 1 pre-existing FU-576 flake). Both DORA_VERIFY History sections deleted per delete-on-pass, one colours/singular-footer visual line stays. Earlier same day, later 8: **verify Batch 3 — P8-05 buy-verdict section fully codified** — 3 new e2e tests (`buy-verdict.spec.ts` → 6: Settings feature-flag off/on across a reload, low-confidence rows stay silent, out-of-stock Buy walk with one-tap add landing on the draft) + new `web_app/test/unit/useBuyVerdict.spec.ts` (7 tests: per-item request cache, 5-min stale window, FU-572 invalidation refetch); frontend Vitest **394/31**, e2e suite 46 pass / 1 pre-existing FU-576 flake. DORA_VERIFY L734–744 deleted per delete-on-pass (one Wait-badge visual stays). Found **FU-580**: the Features-page toggles never refresh the once-per-document health probe, so a flag flip needs a full reload in a live session (fix deferred, opportunistic). Earlier same day, later 5: **real app-driving capability built** — new `web_app/e2e/drive.mjs` (JSON-plan Playwright driver over headless system Chrome: click/fill/viewport/scheme/screenshot steps, auto-login, kills the vite-checker overlay → FU-579 for the underlying pre-existing type errors) replaces the hidden-pane workflow for anything visual; agent Reads the PNGs for real pixels. FU-578 corrected against real rendering: 3 retractions (hamburger nav exists; mobile row-tap navigates correctly; meal-plans skeletons exist), 1 upgrade (System-theme flip → **split light/dark render**, now 7b), new pixel-only findings (icon-only unlabeled nav at all widths, Dora bubble floats over interactive content everywhere, padlock open-toggle metaphor, hard name truncation on mobile rows). Prior same day, later 4: **owner-requested critical UX/UI drive → FU-578** — live app driven (dev backend + quasar dev, DOM/geometry/contrast audit in the hidden pane); 25 findings bundled in FU-578: 7 concrete bugs (open-toggle instant-mutation trap with no cancel, "expires expired" suggestion copy, 375px shopping-list toolbar overflow, mobile nav slivers, peek splitter at phone width, broad light-theme AA contrast fails, unlabelled row button), 15 friction/polish (US dates, "(s)" plurals, raw UUID on Account, blank meal-plans load…), 3 real-browser checks. 🔴 needs owner triage into fix units. Prior same day, later 3: **Zero-Input Pantry HTTP seam codified** — new `tests/e2e/dora_api/test_pantry_beliefs_endpoint.py` (3 tests green; full backend suite **1515 passed**) pins `GET /stock-items/beliefs` default-on shape, the per-user opt-out gate (`PATCH /auth/me` → `{enabled:false, beliefs:{}}` + persistence), and the override-wins pin end-to-end (fresh level change → HIGH-confidence belief); engine was already unit-pinned (`test_pantry_belief.py`). DORA_VERIFY section trimmed per the new delete-on-pass convention (3 bullets deleted, 2 reworded to UI halves). Next Batch-3 target: P8-05 buy-verdict bullets. Prior same day, later 2: **DORA_VERIFY delete-on-pass sweep** — owner delegated deletions; ~85 previously "owner-deletable" lines (Batches 0/2/3: SettingsFileDrop, BuyVerdictCard, Password policy, six backend-pinned Stock fix sections, FU-507/508, stocktake settings + runner codified blocks, bulk-waste, auto-add, 3-band collapse) now actually deleted from DORA_VERIFY.md; convention recorded in CLAUDE.md — sessions delete passed lines at their own close-gate from now on, evidence trail in DORA_VERIFY_TRIAGE + worklog. Prior same day: **3-band StockLevel collapse triaged + server gaps codified** — new `tests/e2e/dora_api/test_stock_level_collapse.py` (6 tests) + 1 import test in `test_data_router.py` pin the section's three untested restock-to-Stocked surfaces: `/finish` flips ticked items to Stocked (unticked keep their level) + `level_overrides` part-restock contract incl. unknown-level 422 no-mutate; alert action `mark_restocked` → Stocked (+`acknowledge_stocktake` contrast: stamp bumps, level untouched); import blank-Level cell → Stocked with "Stocked"/"Low"/"Out of stock" asserted onto sequences 0/1/2. L878/880/881 confirmed already pinned (stocktake-router / assistant alias / cookability tests). Full backend suite **1512 passed**. DORA_VERIFY **L875/877/878/879/880/881 owner-deletable**; L876/882/883 stay V-pack one-time visuals. Next Batch-3 target: Zero-Input Pantry beliefs (L859–872). Prior same day: **FU-577 RESOLVED — auto-add branching matrix backend-pinned** — new `tests/e2e/dora_api/test_update_stock_item_auto_add.py` (12 tests green; full backend suite **1505 passed**) pins the whole server-owned auto-add decision directly against `PATCH /api/stock-items/<id>`: mode × flagged matrix (Off never fires, Essential-only iff flagged, All regardless), Stocked→Out fires like Stocked→Low, transition guard (Low→Out and Out→Stocked both silent), dedup (on the target draft / on a SHOPPING-status list), draft-count guard (0 and 2+ silent; draft+SHOPPING still fires onto the draft). DORA_VERIFY **L845/848/850/851/852 owner-deletable** (adds to later 9's L843/844/847/849/856/857 — the auto-add section is now fully pinned bar L846/853/854/855). Next Batch-3 target: 3-band StockLevel collapse triage (L874–883), then Zero-Input Pantry beliefs. Prior later 9: **Auto-add-on-low first slice codified** — new `auto-add-on-low.spec.ts` (2 tests, serial, self-restoring) pins the install-wide auto-add: Test 1 the Settings → Admin → System → Stock 3-way dial (loads seeded `essential_only`, saves each state eagerly with "Auto-add mode saved." toast, persists across reload, restores); Test 2 the `all`-mode happy path — drop Jasmine Rice Stocked→Low Stock via the row level menu → toast "Added Jasmine Rice to <list>." + "Auto-added because it went low." caption → server truth `added_via=auto_low_stock` on the sole draft → `auto: low stock` chip on list-detail. Scope finding: the webServer seeds `DORA_SEED_BULK_ITEMS=0` so "This week" is the only draft (target unambiguous), but every flagged+Stocked seed item is already on an active list, so the Essential-mode path can't fire on the seed — drove the identical seam via `All items` on a clean non-Essential item instead. DORA_VERIFY **L843/844/847/849 owner-deletable**; server-owned negatives (Off/Essential non-fire, dedup, 0/2+ draft ambiguity — L848/850/851/852) routed to a backend truth-table test via new **FU-577**. Full e2e suite **42 pass / 1 fail** (same pre-existing unrelated uploads channel flake, FU-576). Next Batch-3 target: 3-band StockLevel collapse copy, then Zero-Input Pantry beliefs. Prior later 8: **FU-226 waste section completed — single-item row path codified** — added a 6th test to `bulk-waste.spec.ts` pinning the single-item row expiry-menu "Log waste" path (its own per-item toast `Logged "<name>" as wasted.`, distinct from the bulk summary copy): PATCH an expiry on so the row menu (not the date picker) opens via the `Expires <date>` button, Log waste → Spoiled → server event + expiry cleared → Undo restores both. Whole FU-226 bulk-waste section (L827–836) now behaviourally pinned; DORA_VERIFY L836 owner-deletable (adds to L830/831/833/834/835). Full e2e suite **40 pass / 1 fail** (same pre-existing unrelated uploads channel flake, FU-576). Next Batch-3 target: Auto-add-on-low (FU-315/464/511). Prior later 7: **Verify Batch 3 (Stock B) opens — bulk "Log waste…" FU-226 codified** — new `bulk-waste.spec.ts` (4 tests green) pins the Stock Overview bulk-select waste action: disabled-until-selected, MarkAsWastedDialog copy ("Why did this go to waste?" / "3 items" / one-reason subline), Spoiled tile → plural "Logged 3 items as wasted." toast + one `spoiled` `/waste/events` per item, Undo → "Undone." + events gone, singular copy for one item, and expiry clear→Undo-restore. Self-restoring (UI Undo + an afterAll API hard-reset off `e2e/.auth/user.json`), on 3 seed items no other spec touches. Full e2e suite **39 pass / 1 fail** (same pre-existing unrelated uploads channel flake, FU-576). DORA_VERIFY L830/831/833/834/835 owner-deletable; Batch-3 row ⚪→🟡. Codify-next: single-item row-menu waste (L836), Auto-add-on-low (FU-315/464/511), 3-band collapse copy, Zero-Input Pantry beliefs. Prior later 6: **"Needs check" Overview filter + alert-threshold removal codified** — cleared the Batch 2 codify-next tail. New describe block in `stock.spec.ts` pins the "Needs check" quick-filter (open Filters panel → chip narrows `.stock-row` count to exactly `/stocktake/queue` total, each queued name visible → toggle off restores; "Stocktake (N)" toolbar label mirrors the same server count; read-only, runs before the queue drains). New `alert-thresholds.spec.ts` (2 tests) pins the old "Default stocktake reminder" section is gone from Settings → Admin → System → Alert thresholds (only "Expiring-soon window" survives, still edits/saves/persists on blur; self-restoring to 7). Full e2e suite **35 pass / 1 fail** (the same pre-existing unrelated uploads channel flake, FU-576). DORA_VERIFY L752–753 + L755–760 behavioural bullets owner-deletable. Batch-2 tail now just non-admin banner (L747) + auto→runner cadence effect (L748–749); then Batch 3 (Stock B, L816+). Prior later 5: **stocktake Chunk 3 settings dials codified** — new `stocktake-settings.spec.ts` (3 tests green) pins the Settings → Admin → System → Stocktake page: cadence toggle (Weekly/Fortnightly/Monthly, default Fortnightly) + Auto self-tuning, each tap → toast → PATCH persists across reload; asserted against `/app-settings` server truth, self-restoring so the stocktake queue stays deterministic. DORA_VERIFY L741–746 owner-deletable. Full e2e suite 32 pass / 1 fail (the pre-existing unrelated uploads channel flake, FU-576). Batch-2 codify-next tail = "Needs check" Overview filter + alert-threshold removal. Prior later 4: **stocktake runner Chunk 2 FULLY codified** — `stocktake.spec.ts` grew to 5 tests: added the (?) help-dialog test + a deterministic verb-walk exercising every remaining verb (Skip / Change→Out / Push / Mute / Still-correct) in one queue-drain, asserting the five-counter completion summary + SK-7 add-to-list prompt + Done→/stock. Runner now fully pinned; DORA_VERIFY L792–824 owner-deletable. Suite green bar one unrelated pre-existing uploads channel failure → **FU-576** (bundled Chromium wasn't installable on this box; ran under system Chrome via `DORA_E2E_CHANNEL=chrome`). Batch-2 residual = stocktake *settings* Chunk 3. Prior later 3: **stocktake runner first slice codified** — `stocktake.spec.ts` (5 tests green): redirect, no-landing runner open, Still-correct walk → completion summary → Done, drained→caught-up card; e2e suite now 30 tests; remaining 4 verbs + add-to-list = codify-next. Earlier same day, later 2: **Batch 2 (Stock) first codified increment** — `stock.spec.ts` added (7 tests): FU-508 image-dropped pins + FU-507 expiry-on-open pins, all green; the 5 Stock fix-verify sections confirmed owned by backend regression tests (86 green live) → dispositioned delete, no e2e dup; scan-mode → device pack; stocktake runner + settings → codify-next (seed already yields 4 deterministic overdue items). Full e2e suite now **25/25 green**; ~35 Batch-2 manual checks deletable now. Next: stocktake runner spec, then Batch 3. Earlier: **Playwright e2e suite GREEN 19/19 (~2.6 min)** — the verify-runner unit closed out after two spec-side fixes (bare-array `/shopping-lists` shape; same-document hash-goto doesn't drive vue-router — engineer state via `page.request`, then one full-document goto); Vitest 387/387, vue-tsc clean; the campaign's regression vehicle is live and every UAT round now re-verifies past work via `npm run test:e2e`. Next: Stock batches 2–3. Earlier same day: **verify campaign Batch 0 COMPLETE + 4 real bugs fixed** — the three blocker FUs all fixed + verified live: **FU-571** (CSRF header swept into every raw-fetch mutating call via a shared `csrfHeader()` helper — spreadsheet Import and Backup-restore uploads work again, defence intact), **FU-568** (passphrase hint restored on the 4 auth surfaces), **FU-569** (Windows console logging re-encoded UTF-8 — no more per-request tracebacks under cp1252). Then Batch 0 finished **in-pane** after a harness break-through (the Appendix-D recipe — rAF stub + zero-anim CSS + in-SPA hash nav — mounts the stock detail page after all): SettingsFileDrop 5/5, **BuyVerdictCard L87–92 all pass** across overview/detail/list-line, Runtime URL walked. Mid-walk found + fixed **FU-572** (buy-verdict client cache: invalidate never refetched mounted cards + cart quick-add seams never invalidated — cards now repaint in place; toast copy de-hardcoded to the real level name). New open findings: **FU-573** (remove-from-all toast over-counts server no-ops), **FU-574** (bogus runtime backend URL leaves a browser user stuck — error page has no change-URL escape hatch, DORA_VERIFY L113 recovery clause FAILS). Owner can delete DORA_VERIFY L76–80, L87–92, L91–98 (after the L91 example rewrite), L95, L112. Next: Playwright verify-runner (FU-540 scaffolding) for screenshot/V-pack batches, then Stock batches 2–3.) Prior: 2026-07-16 (hand-edited: **verify campaign Batch 0 continued** — dedicated verify DB stood up (root dora.data.db reseeded via the app's own dev path; pilot's dashboard 500s confirmed as pure schema drift), Password-policy section COMPLETE (7/8 pass; L91 example needs rewrite; L95 fail stays FU-568), SettingsFileDrop 3/5 verified with the tail blocked on **FU-571 (new real bug: chunked uploads always 403 — useChunkedUpload's raw fetches skip the CSRF header, so spreadsheet Import and Backup-restore are hard-broken in any browser; fix recommended now)**, BuyVerdictCard mark_stocked state engineered + API-verified ("QA Verdict Cheese" fixture persists), and a campaign-shaping harness finding: the preview pane runs hidden (no paint/rAF) so Vue-transition-gated UI can't mount — UI walks move to a Playwright-driven runner next session (repo already has FU-540 scaffolding). **DORA_VERIFY campaign launched** — the ~1,406-item QA pile was fully triaged by 7 parallel agents into `DORA_VERIFY_TRIAGE.md` (new repo-root campaign tracker): 92% agent-verifiable, 66 owner-eyeball items bundled into walkthrough packs, 40 hardware-only items grouped into device packs (Android/iPhone/mac/mic), 61 stale-suspect items with evidence awaiting owner delete calls; 20-batch session plan; pilot run on the Password-policy section verified 5/8 checks live and found 3 real issues → FU-568 (passphrase fineprint regression, lost in C-19 rebuild), FU-569 (Windows console logging spam), FU-570 (silent boot on stale-schema DB); next verify session starts by standing up the dedicated seeded verify DB. FU-431 Product History RESOLVED — decided **no redesign brief**: PH-10's desktop bottom-drawer already exists (`PriceHistoryBottomSheet.vue`, built for FU-227, on stock-item detail), PH-1 discoverability is fine via contextual entry (My Products/Subscriptions/onboarding — a nav tab would surface an often-empty data-gated page, Anti-creep), and FU-227's "your prices" personal-price layer subsumes the primary need; product-side history is niche + data-gated. Residuals (PH-2 verify, optional bottom-sheet reuse on My Products, entry-point adequacy) folded into FU-214's product-surface pass; decision unit, no code. FU-566/telemetry RESOLVED WON'T-DO (self-host) — after an "is this the most useful lens?" review, owner decided not to build any telemetry for self-host and to migrate the whole topic into the optional SaaS plan (it's the maintainer's question → hosted aggregate only; the self-host operator wants outcome/health signals already derivable from domain data, not feature-engagement stats). `PROPOSAL_USAGE_TELEMETRY.md` re-statused 📦 parked→hosted-only; OPTIONAL_SAAS §3 owns it; COVERAGE_GAPS flipped to DEFERRED→hosted-only; docs only, no code. FU-412 self-host commercialization plan written (`docs/04_proposals/SELF_HOST_COMMERCIALIZATION_PLAN.md`) — synthesised the report against current code (its robustness program is already done: RapidFuzz/Postgres/gunicorn/security-headers/scraping→companion), so selling self-host is now a legal+billing+packaging job, not engineering; tracks linked to FU-562 (billing) / FU-406+FU-557 (launch), with DSAR/compliance (FU-404) rescoped as a hosted/controller obligation (parked in OPTIONAL_SAAS — self-host keeps only a privacy statement); spawned **FU-567** (relicense off MIT — MIT permits resale, undermining the paid-update gate; the recommended first step); planning-only, no code. FU-363 cross-cutting bundle — 4 of 8 items actioned: item 6 menu-border removed (code, `MainLayout.vue`), item 7 ALDI/IGA logos declined WON'T-DO (trademark; upload already covers it), item 5 Kivy P2P sync decided CUT (`MULTI_USER_READINESS.md` §5.1), item 2 telemetry designed (new `PROPOSAL_USAGE_TELEMETRY.md`, build→FU-566); FU-363 stays open for the other 4. FU-394 doc-only scope confirmation — verified no part of legacy prompt P5-10 (merchant data quality & support bundle) landed in Dora-core, and added a MOVED/PARKED banner splitting its two halves: merchant-quality → companion (Decision 1), support-bundle → parked-unbuilt core ops tooling (distinct from the shipped FU-370 report-an-issue channel); no code. FU-373 shipped the register-barcode-against-Product UI on My Products — `scanningEnabled`-gated `⋮` "Register barcode…" + text-entry dialog over the pre-existing `POST /api/data/barcodes` endpoint; §6 tab-placement was already resolved and scan-unknown rework is a deliberate WON'T-DO (My-Products-only, text-only scope calls), closing FU-373 bar the §5.2 ingestion auto-populate remainder; frontend-only, Vitest 387/30. FU-552 shipped real Dora-branded PWA icons for the six paths Quasar's `injectPwaMetaTags` hard-references (iOS `apple-icon-*`, `ms-icon-144x144`, `safari-pinned-tab.svg`) — the full-PWA build was 404'ing/placeholdering them on iOS/Safari; all now resolve to committed Dora assets (14/14 built-index paths OK); assets-only, no code. FU-565 6 FK ondelete drifts reconciled (migration `d3f8b1a6c4e2`), schema-match test now also compares FK ondelete — **the FU-393 data-model sweep is fully closed** (indexes/nullability/ondelete all reconciled + enforced), backend 1490 green. FU-564 Product nullability reconciled to the model (migration `c1e8a5f3d9b2`; is_active/is_available→NOT NULL, merchant_stockcode→nullable), schema-match test now enforces it, backend 1490 green — only FU-565 ondelete drift remains of the FU-393 sweep. FU-563 data-model reconciliation — mirrored 32 prod indexes + 4 uniques into the ORM model, added 43 FK covering indexes (model + one additive migration `b9d4f2a7c3e1`), strengthened the schema-match test to compare columns+nullability+index colsets; backend 1490 green, R-034/ADR-030 promoted; ondelete drift → FU-565, nullability stays FU-564 (now test-guarded). FU-545 SettingsFileDrop accessible label-wrap refactor — killed the `nested-interactive` a11y violation, re-enabled empty/filled axe assertions, frontend Vitest → 387/30, tree vue-tsc-clean; FU-520 `useOfflineQueue` composable spec — item 3's last surface drained, only Postgres CI (FU-405-gated) left). Prior 2026-07-14 (hand-edited: FU-520 Postgres test-isolation rewrite + 2 SQLite-vs-PG divergence bug fixes + shopping-rail/useMealPlanExport/companion-scraper specs (backend green on PG & SQLite 1490 each; frontend → 372), FU-520 authStore Vitest spec (frontend suite → 360/27), FU-393 data-model sanity sweep (drift + FK-index findings → FU-563/564), FU-397 production WSGI server (gunicorn) built, FU-378 action-first Stock Overview scan mode built, self-host-first commercialization split + OPTIONAL_SAAS_AND_MANAGED_DEPLOYMENT doc, FU-410 folded into MULTI_USER_READINESS §5, FU-413 email-setup verified+closed, FU-556 seed-builder DRY, FU-561 migration-test isolation fix, FU-560 bundle-analysis + AppSetting memoisation, FU-388 perf sweep clean + dev-seed-under-load, FU-559 test fix, FU-376 products-overlay residuals, FU-370 support channel dormant, FU-348 import-template symmetry guard; see Recently shipped). Prior full regen 2026-07-12 (test pass 4)** — fourth
test-sweep pass: backend **1415 passing** (~61 s; 1 pre-existing FU-328 fail,
9 deliberate strict-xfail pins), frontend **298** Vitest tests (19 files).
Pass 4 added whole-API **input fuzzing** ("4xx never 500"), a **PATCH-semantics**
suite (null-out matrix across 9 surfaces), the **Pinia store layer** (4 stores),
and closed the FU-519 e2e tail (suggestions/substitutes; deals has no HTTP
surface). **Found 7 real bugs, fixed all 7 inline:** two endpoints that 500'd on
*every* request (`recipes/unlinked-ingredients`, `meal-plans/reconcile-queue`
pagination) and the **rename-to-own-name 422 at 5 surfaces** (every edit dialog
failed to re-save without a name change). Two more logged for architecture calls:
[[FU-532]] (82 routes 500 on a non-UUID path param — uuid-converter ADR) and
[[FU-533]] (three noload-read PATCH bugs incl. silent consumption-event loss).
**Two ADRs are now DUE** — the noload include-discipline rule (FU-527 trigger has
fired) and the uuid path-param converter rule (≥4 str/UUID incidents) — teed up
in the FUs for product-owner sign-off. **Both top user-visible bugs now fixed + closed — [[FU-533]] (consumption
loss) and [[FU-526]] (stocktake snooze 500 / bell crash on SQLite).** Both ADRs are now
written (R-032 noload include-discipline; R-033 uuid path-param converter) and
R-033 applied end-to-end (FU-532 done). Highest-value remaining work is
test-strengthening — FU-534 query budgets (the noload regression net) and FU-539
frontend resilience. Prior refresh:
**2026-07-11 (test pass 3, hand-edited refresh)** — third
test-sweep pass: backend **1323 passing** (~53 s; 1 pre-existing FU-328 fail,
5 deliberate strict-xfail pins), frontend **254** Vitest tests (15 files).
Pass 3 added the **route auth/permission enforcement sweep** (all 271 /api
rules; anonymous 401 + pinned 32-endpoint admin-403 contract — **no
vulnerabilities found**), a **delete/referential-integrity suite** (24
cross-entity delete webs), the FU-519 minor-surface tail (locations tree /
app_settings / client_logs / help, 55 tests), and 4 component specs incl. the
queued AddToListButton. **One real user-visible bug found + fixed: product
unlink from a stock item always 404'd** (third FU-528 str/UUID incident —
family now at ADR-candidacy threshold; browser walk queued in DORA_VERIFY).
Two suite-robustness fixes (date-sensitive digest dedup rewrite; in-memory
rate-limiter drain in the sweep). New findings: [[FU-530]] duplicate admin-gate
copy, [[FU-531]] component nits. **[[FU-526]] (stocktake snooze 500) remains
the top fix on the board.** Prior refresh: **2026-07-10 (pass 2)** — after the second test-sweep pass the
suite stands at backend **1238 passing** (~45 s; 1 pre-existing FU-328 fail,
5 deliberate strict-xfail FU pins) and frontend **203** Vitest tests. Pass 2
added: search e2e, **DTO contract snapshots** (27 endpoints pinned against a
committed `dto_snapshots.json`), 103 long-tail e2e tests (waste / budget / all
10 reports / stocktake lifecycle / 6 taxonomy routers), and the **first
component-level Vitest** (both StockLevelDots; Quasar mount pattern now
established). Four more findings logged: **[[FU-526]] stocktake queue 500s +
alerts bell breaks while a snooze is active on SQLite (fix next)**, [[FU-527]]
noload count bugs ×2 (third noload incident today — ADR candidate), [[FU-528]]
str/UUID delete-count mismatches, [[FU-529]] reconcile receipt-ordering flake.
FU-519 is now essentially done (minor-surface tail only); FU-520's only
remaining item is **Postgres CI wiring (gated on FU-405)** — item 3's last
composable (`useOfflineQueue`, 13 tests, 2026-07-15) is now covered, and the
suite already runs green on Postgres via the `DORA_TEST_DB=postgres` selector.
Frontend Vitest now **385 tests / 30 files**. Prior refresh: hand-edited row refresh after the
**FU-519/FU-520 targeted test sweep (2026-07-10)**: backend suite **920 → 1096
passing** (~41 s), frontend Vitest **53 → 192** (~1 s, 9 new spec files over the
pure utils/composables). New suites: recipe/meal-plan/dashboard router e2e
(search still open), `SqlAlchemyRepository` operator/paginate suite, emailer
render+send with fake transport, Hypothesis property invariants. The sweep
found **one user-visible production bug — the cookbook `?cookable` /
`?max_missing` / `?expiring_within_days` filters were silent no-ops
(identity-map/noload poisoning in `_restrict_query`) — fixed + test-pinned +
browser-walk queued in DORA_VERIFY** — plus four logged findings: [[FU-522]]
change-email plain-text wrong link, [[FU-523]] Contains/StartsWith
`case_sensitive` inversion, [[FU-524]] `normalise_unit` non-idempotence,
[[FU-525]] buy-verdict UTC/household clock mix. [[FU-518]] closed (per-key
dedup rewrite + `GetAlertsHandler` `now` seam). FU-519/FU-520 bodies now carry
shipped-vs-remaining status; remaining slices (search e2e first, long-tail
surfaces, contract snapshots, component Vitest, Postgres CI) deliberately
deferred per user direction — suite scaled to the app, picked up per-touch.
Prior refresh: hand-edited row refresh after **FU-357 CLOSED
end-to-end + bell crash root-caused and fixed (2026-07-10)**: user's browser
walk on the FU-357 verify steps hit a real `ErrorBoundary` crash on the alerts
bell (`TypeError: all is undefined` at `AlertRow.vue:114`). Static-read root
cause: **FU-317 Chunk 4 (2026-07-09) added the `meal_reconcile_overdue` alert
kind on the backend but never extended the SPA's `AlertKind` union in
`alert.ts`.** Since the union didn't include it, TS couldn't warn about the
missing case in the five kind-switches (`iconFor` / `colorForKind` /
`kindTheme` / `actionsFor` / `linkFor`) — all fell through and returned
`undefined`, so `actionsFor(kind).slice()` threw and blew up the bell. Fixed
by (a) extending the union + adding a case to all five switches with sane
defaults (nav-only nudge, icon `playlist_add_check`, link
`/meal-plans/reconcile`), (b) defensive `?? []` at `AlertRow:112-118` so the
next backend-only kind rollout degrades to a nav row instead of crashing.
Same session also fixed a second finding — **Dashboard alert-action was
silently succeeding/failing** (no `$q.notify` call), so `AlertsBell` +
`AlertsPage` toasted "Done." but Dashboard didn't; parity restored. **FU-357
closed** (the reported "cross-app undo seems off" turned out to be this toast
inconsistency, not an undo — no undo exists on the push_expiry path
anywhere in the SPA, confirmed by grepping every `label: 'Undo'` site). New
**FU-521** logged (state-ownership drift — three hand-rolled alert-action
wrappers + five parallel kind-switch lists) for the finalisation plan Chunk
9 senior review to consolidate. Prior regen note: **Finalisation
plan drafted (2026-07-10)**: new [docs/01_charter/FINALISATION_PLAN.md](docs/01_charter/FINALISATION_PLAN.md)
+ companion [FINALISATION_COVERAGE.md](docs/01_charter/FINALISATION_COVERAGE.md)
lock the shape of the end-of-project sweep — 20 feature chunks × 4 output tracks
(FST procedure doc, in-app help & guides, senior code review, test plan
inventory). Each chunk gets one deep read with all four tracks advanced from
the same walk; coverage register keeps it honest. Rolled 8 FUs into it: **FU-361
A-4 help content overhaul / FU-320 auto-behaviour documentation / FU-395 P5-11
production readiness review CLOSED end-to-end** at plan draft (moved to
`_RESOLVED` — full scope absorbed into the plan, plan is the sole tracker for
their content). **FU-510 fully absorbed into the plan (retired 2026-07-15)** — both phases now
live in §3.3; the per-swap Phase-2 FUs are guaranteed by the plan's DoD step 5
(hard close-gate), so there's no standing FU.
**FU-406 P7-10 launch readiness + FU-404 P7-08 compliance** stay open with
partial-coverage notes (FST doc handles the QA/exercise halves; legal +
marketing + on-call + DSAR drafting stay out). **FU-010 holistic theme review +
FU-224 colour-usage audit** stay open, riding along as per-chunk signal
collection — the plan populates their shortlists but eyes-on-app judgement
is still needed to close them. No code touched. Prior regen note: **FU-169
CLOSED end-to-end (2026-07-09)**: the earlier Phase-1-tail + Phase-2 land got a
close-out pass — FU-518 root-caused (not fixture-ordering; a freshly-POSTed
expired item generates *both* `stock:{id}:expired` + `stock:{id}:low_stock`
alerts, so dedup misses the second key — test-design flaw, not a Phase 2
architecture issue; xfail marker updated with the finding); parametrize
sweep collapsed the `PageValueIsNotInteger`/`LimitValueIsNotInteger` pair
in five router files into a single `@pytest.mark.parametrize`d test each
(10 → 5 test functions, no coverage loss); Phase 3 (coverage gaps / untested
API surfaces / repository + contract tests) spun off as **[[FU-519]]** and
Phase 4 (frontend Vitest / Hypothesis / scraper + emailer fixtures /
Postgres CI) spun off as **[[FU-520]]** so each can be sequenced
independently. FU-169 itself archived to `DORA_FOLLOWUPS_RESOLVED.md`
with a one-line close note. Prior refresh: a substantial
**FU-169 Phase 1 tail + Phase 2 close-out (2026-07-09)**: `pytest-cov` wired
(report-only, no gate); **per-test DB rollback via SQLite file snapshot**
(the session-scoped `api` fixture snapshots the seeded DB, autouse teardown
rolls back the ORM session + disposes the engine + restores the snapshot
in ~1-3 ms); `tests/factories.py` with `make_stock_location` /
`make_product` / `make_stock_item`; new `uuid_bind` helper in
`tests/support.py`. The pass surfaced + fixed **two real bugs from earlier
today's FU-317 chunks**: (a) `bump_pool`'s `str(uuid)` bind blew up on
SQLite's BINARY(16) UUIDType columns on the ORM caller path (`cook_recipe`)
— fixed via a shared `_id_bytes` normaliser; (b) `_sweep_auto_drain`
lacked the `NOT EXISTS` receipt-guard the manual branch has, so a
`Didn't cook` verb that cleared `consumed_at` triggered the next sweep to
re-drain the entry over the user's decision — guard added. Also hardened
a pre-existing safety hazard (`os.environ.setdefault("DORA_DB_PATH", ...)`
was silently letting `.env` route tests at the dev DB → clobbering it).
Suite state at close: **921 passed / 1 xfailed ([[FU-518]] — new: flaky
digest dedup test only fails during whole-file collection) / 1 failed
(FU-328 pre-existing CSV template hint-row shape, out of scope)**, ~22 s
full runtime. FU-169 remaining tail (parametrize sweep, CI un-comment,
`assert_problem` retrofit, Phase 3/4) stays deferred with a clear
recommended-resolution note. Prior refresh: hand-edited row refresh after **FU-317 Chunk 6
close-out (2026-07-09)**: new **Settings → Admin → System → Meal reconciliation**
page (single install-wide *auto-drain past-day meals* toggle wired to the existing
`AppSetting.auto_drain_past_meals` column + a deep-link chip to the reconcile page
for everyone); Settings left-nav row added; frontend `AppSettings` type extended
to expose the field. That closes the FU-317 impl stack end-to-end — Chunks 1-6
all shipped in a single day. `vue-tsc` clean for touched files (the three
pre-existing DashboardPage `'draft_shop'`/`CardId` errors are unchanged and
unrelated). Prior refresh: hand-edited row refresh after two more polish
units later the same day: **FU-359 A-2 data-pages UI revamp** (Import + Backup
& restore rebuilt onto the shared Settings design language via a new shared
`SettingsFileDrop.vue`; presentation-only, `vue-tsc` clean; FU-359 →
RESOLVED) and **FU-360 #3 mode-slider** (Dora chat header's read-only
Basic/AI chip replaced with a two-position `DoraModeSlider.vue` — tap flips
the user's `llm_enabled` and re-probes; disabled state carries an
explanatory tooltip when the install master's off or the user hasn't
finished configuring a provider; FU-360 remaining tail is #1/#4 browser
verifies only) — plus earlier same day **FU-407 / RD-18** (per-line "Swap
with substitute" on shopping lists disabled up front when the item has no
recorded substitute, via a new server-derived `has_substitutes` flag; also
relabels to disambiguate from the "store offers" picker). Earlier tail on
this date: **FU-387 P5-01 security & privacy hardening bundle sweep landed
end-to-end** (two-session split
— session 1 audited the six P5-01 buckets and produced a greenlit slate,
session 2 executed it). Shipped: prod-default `SESSION_COOKIE_SECURE=True`
(env stays as explicit opt-out), pinned scrypt password hashing via a new
`hash_password()` helper routed through all eight call-sites, backup
credential-exclusion widened (User `llm_api_key_encrypted` + AppSetting
`smtp_password_encrypted` + `vapid_private_key_encrypted`), full Python dep
bump clearing 16 CVEs (`flask-cors 4.0→6.0`, `flask 3.0.2→3.1.3`, `jinja2
3.1.2→3.1.6`, `requests 2.32.4→2.33.0`, `pytest 8.3.4→9.0.3` + compat pins),
`npm audit fix` resolving 3 of 5 frontend vulns (form-data/vite/js-yaml; 2
low-severity dev-only Windows-only esbuild transients accepted pending
upstream Quasar), new [SECURITY.md](SECURITY.md) at repo root, new
[docs/security/SECURITY_REVIEW.md](docs/security/SECURITY_REVIEW.md) as the
standing per-bucket audit doc, new [scripts/security-audit.sh](scripts/security-audit.sh)
on-demand runner. `pip-audit -r requirements.txt --strict` clean. Slate items
dropped by user during execution: P3 per-account lockout, P5 user-isolation
suite (correctly — single-household model has no user-vs-user boundary inside
a household). [[FU-387]] → RESOLVED; [[FU-405]] gained a "promote
security-audit.sh to CI at Phase 4" note.** Earlier same day: **`good_deal`
alert type ripped end-to-end (product-owner call): the alert kind, per-user threshold
column, Preferences → Notifications control, AlertsPage mapping, and migration
`d2f8a1c4b7e9` all removed; `--alert-kind-good-deal` renamed to `--savings-accent`
(still used by the swap-suggestions panel + dashboard savings signpost). Reason:
proactive "product is cheap right now" nudges read like the app pushing users to
buy from stores, which isn't Dora's posture. `DealQuality` compute + its use by
Buy Verdict (fake-markdown demotion) + the FU-451 swap-ranker fake-markdown
filter all unchanged. FU-516 (throttle deviation) deleted — the feature it
described no longer exists. `e3a9c7b1f2d8` rewired to `c7d1a9e3f2b6` as its
down_revision.** Earlier same day: **FU-450 + FU-451 shipped end-to-end (6 chunks):
the deal-quality signal (band enum + `fake_markdown` demotes a price-driven Buy
Verdict `buy`→`wait`) and budget-defense **recipe** swaps (over-budget week →
ranked cheaper-recipe suggestions, preview→apply→undo via `MealPlanSwapLedger`,
migration `e3a9c7b1f2d8`; Suggestions panel + Dashboard signpost). **Product/brand
swaps CUT** — they needed per-item "usual product" upkeep the owner rejected.
Recipe-cost pricing extracted to a shared `recipe_cost` module (R-003)**. Also **FU-432 resolved** — recipe
personal notes shipped (RD-29, migration `f4b2d8e6a1c3`, shown in cook mode under the
steps); RD-11 per-ingredient notes dropped (anti-creep), RD-3/RD-33 verified
already-done, RD-18 dup of FU-407. Earlier: **FU-515 assistant security triage (B.1 prompt-injection defence + B.3 tool-arg bounds + B.4 current_path sanitisation shipped; B.2/A.3/A.4 verified already-fixed; A.5/A.6/A.7 accepted; audit doc "✅ Fully triaged")**. Earlier same day: **FU-390 assistant eval suite (vitest stood up in web_app; 53-case Basic-mode corpus + 9 AI-path reliability tests; caught+fixed 3 real bugs incl. an orphaned `set_primary_list` tool) and FU-514 (onboarding seed-items 500 fix)**. Earlier same day: **FU-429 assistant-architecture reconciliation (SLM has landed → the big "capability registry + two routers" refactor deliberately NOT built per Effortless+Anti-creep; instead closed the highest-value gap by giving Basic mode its first action verb — typed "add milk"/"buy eggs and bread" now resolves against the pantry and adds to the primary list; §1 dup + §2.1 registry + mutation gate already satisfied. Related: FU-386 closed already-honoured, FU-390 eval suite unblocked+re-scoped, FU-360 subset shipped — hide-Dora setting, per-user greeting, chip sizing)**. Earlier same day: **FU-447 close-out (already-satisfied by [[FU-197]] 2026-06-30 — A.1 CSRF + A.2 email-change password-proof both fixed and pinned by tests; AUTH_ASSISTANT_SECURITY_FINDINGS.md doc header + priority table stamped with per-finding status; remaining 8 medium/low items carried forward as new FU-515)**. Earlier same day: **FU-512 close-out (unit-of-work sweep applied to 10 more handlers — `CreateMealPlanTemplate`, `CloneMealPlanTemplate`, `CreateSet`, `NewRecipeVersion`, `AutoGenerate`, `Seed` onboarding, `CopyShoppingList`, plus 3 shopping-list-template handlers; habit-commits deleted, `flush()` used where child inserts need FK visibility; 10 new happy-path e2e tests, all green; surprise pre-existing bug found in `SeedItemsHandler` logged as FU-514)** and **FU-513 close-out (GET /api/suggestions writes eliminated; snooze pruning moved to a daily APScheduler job)**. Earlier same day: **DI walk-back (R-031 / ADR-027 — `DependencyContainer` deleted; 175 handlers rewritten to constructor injection typed against a `Repository` Protocol; ~200 `get_container().inject(X)` callsites rewritten; `dependency_injector` dropped; FU-457 dissolved by construction; 828 pass / 2 pre-existing fail — 0 regressions)**, **FU-456 (`create_recipe` unit-of-work refactor — 5 commits → 1, 3 new e2e tests, FU-512 opened for the sweep of remaining multi-commit handlers — now closed)**, **FU-500 (R-029 hide-don't-nag sweep — Product Search nav flipped to hide, MenuButtonProps shed disabled fields, 17 R-014 comment refs audited)**, and **FU-504 (base-component sweep — 40 raw `<q-btn>` migrated, `BaseButton` gains `filled-icon` variant + `color` prop)**. Prior refresh: **2026-07-06** — hand-edited row refresh after **FU-333 close-out (env-var sprawl finished; Buckets C + D + fallback drop)**, **FU-502 (locale/currency backend coverage tests)**, **FU-503 / FU-044 (help chips shipped)**, and **FU-043 (Locale
& international readiness Layers A + B)** shipped end-to-end: install-wide
currency + locale on `AppSetting`, one shared `Intl.NumberFormat` wrapper
routing every money render, voice-input locale following the setting, AU
merchant name-drops removed from assistant copy, new admin **Settings → System
→ Currency & locale** page. Also earlier the same day: **FU-003** resolved by
reverting the Pesto `--text-muted` bump instead of propagating it. Baseline
below is unchanged from the 2026-07-04 full rebuild.

**Original regen note (2026-07-04):** — full dashboard rebuild after a long session
that shipped the **Stocktake mode redesign trilogy** (Chunks 1–3
end-to-end from `PROPOSAL_STOCKTAKE_MODE.md`), the **stocktake
housekeeping pass** (retired the dead `locations/attention.py` heatmap +
dropped two deprecated columns via alembic `e5c8b3a1f4d2`), and
resolved **7 follow-ups**: FU-226 (queue-rules assessment → became the
proposal), FU-430 (redesign brief written), FU-455 (buy-verdict
all-thin-axes test drift fixed), FU-464 (auto-add-when-low regression
from the 3-band collapse), FU-463 (two more SQLite `text()+str(uuid)`
zero-row sites rewritten), FU-458 (per-user rate limits on
`/assistant/ask` + `/act` + `/confirm`, extending `auth_helpers.rate_limit`
with an optional `subject` arg), and FU-421 (closed as
already-satisfied — existing expiry surface covers the "remind me to
use this" spec ask). Backend pytest: **303/303 green** (was 297/298 at
session start — 5 new tests, 1 pre-existing failure eliminated).
`vue-tsc --noEmit`: clean throughout (only pre-existing Capacitor
typing errors from the P8-10 native scaffold on this dev box).
Migration head-count: 1 (was 2 — Chunk 1's `d1f9c3a8b2e4` merged the
divergent 07-03/07-04 heads and Chunk-house `e5c8b3a1f4d2` chained
cleanly). Session's front-door is the single **PROPOSAL_STOCKTAKE_MODE.md**
brief; every decision it locks is live in code.

This is the single front door: where every phase and workstream is up
to, and what needs your attention. For *where things stand* this doc
wins; for *how/why* a decision was made, follow the linked planning
doc. Regenerated after each substantive close-gate (see
`CLAUDE.md → Regenerating PROJECT_STATE.md`); if it looks out of date,
the last session skipped its close-gate — trust `DORA_WORKLOG.md` +
`CHANGELOG.md` over it.

Status key: ✅ done · ➗ done, with skipped/deferred items · 🟡 in progress ·
🔵 designed, not built · ⚪ not started · 🔴 needs your decision · 🕸 stale doc.

---

## Where we are right now

Phases **0** and **2** are effectively done: foundations, and the
ingestion API + standalone companion. **Phase 1** just closed a big
outstanding piece — the **stocktake-mode redesign shipped end-to-end**
(new engagement gate + cadence bands + Auto self-tuning; runner rebuilt
around Still-correct/Change-level primaries + Skip/Push/Mute
secondaries + completion-screen batch add-to-list; Settings block +
Stock Overview "Needs check" filter + pulsing outline on overdue rows).
That closes SK-1..11 from the feedback pass; loop-progression items
FU-450 + FU-451 shipped 2026-07-09 (deal-quality + budget-defense recipe swaps);
FU-452/351/352 remain. **Phase 3 sits at ~95%** — the champion
sequence `P8-01 → P8-02 → P8-05 → P8-06 → ⭐ P8-07 → P8-08 → P8-09 →
P8-10` is fully built. Nothing champion is left to construct; three
surfaces still need a browser walk (P8-07 flagship, P8-08 Kitchen
health card + P8-09 Memory reports, P8-10 Native Android).
**Next up:** either a **browser-verify sweep** across the pending
surfaces (P8-07 flagship + the two new stocktake redesign blocks +
P8-10 native), or **Phase 4 kick-off** (commercialisation report →
per-recommendation FUs; email setup; multi-tenant readiness). Also
worth grabbing while surfaces are open: FU-464 auto-add re-verify
(fixed this session but e2e-only coverage) and FU-355 sign-out
list-state clear.

---

## Phase board

| Phase | Scope | Status | Remaining |
|---|---|---|---|
| **0 — Foundations** | Theme/buttons/modals/filters/text-size/renames + bug clusters + config/opt-ins | ✅ ~99% | Residual polish clusters (FU-360/363/431/432); FU-359 + FU-421 + FU-430 + **FU-361 (help content — absorbed into FINALISATION_PLAN)** closed to `_RESOLVED`. |
| **1 — Close the loop** | Shopping lists, cook mode, stock overview, cookbook, suggestions, costing, stocktake | ➗ ~90% | **Stocktake mode redesign shipped end-to-end** (Chunks 1–3 + housekeeping); SK-1..11 resolved. **FU-449 closed** — P6-07 cook→consume `ConsumptionEvent` persists. **FU-351 shipped** — P6-10 "Draft my shop" one-click dashboard card on top of the existing `/auto-generate` engine (2026-07-07). **FU-352 closed** — P6-12 briefing / P8-08 Score coexistence decided (2026-07-07: keep both cards, no fold). Remaining P6 gaps: **FU-450** P6-03 `fake_markdown` (shipped; `good_deal` alert cut same day); **FU-451** P6-09 budget-defense swaps; **FU-452** P6-11 put-away + expiry-by-location grouping. |
| **2 — Ingestion API + companion** | `/api/ingest` seam; extract scraper to standalone companion; Merchant→Store rename | ✅ done (backend-green) | Browser-verify pending (FU-214 + Phase-0/F verify FUs). |
| **3 — Champion** | Zero-Input Pantry (flagship), buy/wait oracles, barcode-add, Dora Score, culinary memory, native app | ➗ ~95% (verify pending) | **Champion sequence P8-01..P8-10 complete.** ⭐ P8-07 Zero-Input Pantry, P8-08 Dora Score, P8-09 Culinary memory, and P8-10 Native mobile app all BUILT (Capacitor 8 wraps the SPA; Android scaffolded locally, iOS scaffolded for a Mac session; runtime backend URL + first-run gate; wake-lock in cook + shop mode). P8-01/02/05/06 shipped earlier. P8-03 + P8-04 formally cut (§7 Decisions 6/7). Only remaining: browser-verify the four unverified surfaces (P8-07/08/09/10). Native FCM push deferred as [[FU-465]]. |
| **4 — ~~Commercialize~~ → Open-source release** | ~~Tenancy, billing, compliance~~ → README/showcase + release process + support channel (Postgres already done) | ⚪ ~0% | **Not being sold (reversed 2026-07-31): donation-funded, open-source, MIT kept, all features free.** Billing/tiers/relicense are **won't-do** (FU-562/FU-567 RESOLVED); no billing code ever existed. Remaining Phase-4 work is **open-source *release* readiness** — FU-406 (README + GitHub Releases process + Sponsors link) + FU-557 (best-effort GitHub-issues support). **Postgres done + default** (FU-045). SaaS/hosted parked (OPTIONAL_SAAS). |

---

## Major workstreams

| Workstream | Status | Where it's at | Governing doc |
|---|---|---|---|
| Products-as-overlay | ➗ | Phases 0–F code-complete; tail (FU-210, FU-214 bulk-select/hard-delete) **browser-verify pending** | [RUNBOOK](docs/04_proposals/PRODUCTS_OVERLAY_RUNBOOK.md) |
| Ingestion API | ✅ | Backend green (401/401 tests), bearer-auth lane, admin keys page | [PROPOSAL](docs/04_proposals/PROPOSAL_INGESTION_API.md) |
| Standalone companion | ✅ | Runs standalone (`../dora-companion`); round-trip tests pass | RUNBOOK Phase C |
| Merchant→Store rename | ✅ | `usual_store_id` + Stores page shipped | RUNBOOK Phase E |
| Shopping Lists | ✅ | 8/8 bullets; DRAFT→SHOPPING→DONE loop | [PROPOSAL](docs/04_proposals/SHOPPING_LIST_REDESIGN_PROPOSAL.md) |
| Cook Mode | ✅ | Chunks 1–6 (84% of feedback) | `C_big_rock_design_briefs.md` |
| Cookbook | ➗ | Chunks 1–10 shipped; 5 Recipe-Detail bullets remain (FU-432); tag-taxonomy needs env verify (FU-085) | [PROPOSAL](docs/04_proposals/PROPOSAL_COOKBOOK.md) |
| Stock Overview | ✅ | 32/41 bullets; 3-band StockLevel; buy-verdict badge wired; new "Needs check" filter + pulsing overdue outline (FU-226 tail) | `PROPOSAL_STOCK_OVERVIEW` |
| **Stocktake Mode redesign** | ✅ | **Built end-to-end 2026-07-04.** Chunk 1 backend engine (new engagement gate, cadence bands, Auto self-tuning, grace-period baseline, Push snooze endpoint) + Chunk 2 SPA runner (Still-correct/Change-level primaries + Skip/Push/Mute secondaries + `(?)` help + completion-screen batch add-to-list; landing page retired) + Chunk 3 Settings + Overview surfacing. Housekeeping pass extracted `resolve_overdue_map` shared authority for alerts feed (was a two-authority R-003 drift) and deleted the dead `locations/attention.py` heatmap system. Alembic `d1f9c3a8b2e4` + `e5c8b3a1f4d2`. 18/18 cadence pytest green. SK-1..11 feedback resolved. | [PROPOSAL](docs/04_proposals/PROPOSAL_STOCKTAKE_MODE.md) |
| ⭐ Zero-Input Pantry (P8-07) | 🟡 | Built end-to-end (belief service `pantry_belief.py` + `ConsumptionEvent`/FU-449 + `/stock-items/beliefs` + additive `PantryBeliefChip` + `pantry_check` quick-check + per-user pref). **Browser verify pending.** | [PROPOSAL](docs/04_proposals/PROPOSAL_ZERO_INPUT_PANTRY.md) |
| Buy-verdict oracle (P8-05 + P8-06) | ✅ | Row + shopping-line badges + full `BuyVerdictCard` wired into stock-item detail overview (FU-437 closed); P8-06 `wait_hint` on `wait` verdicts (FU-438 closed); one-tap `mark_stocked` + `remove_from_list` actions wired (FU-454). | [PROPOSAL](docs/04_proposals/PROPOSAL_BUY_VERDICT_ORACLE.md) |
| Barcode-to-add (P8-02) | ✅ | OFF lookup for unknown EANs, gated by `scanning_enabled` | [PROPOSAL](docs/04_proposals/PROPOSAL_BARCODE_SCANNING.md) |
| Native mobile app (P8-10) | 🟡 | Capacitor 8 wraps the SPA; Android scaffolded on this Linux dev box (adaptive icons, `CAMERA/WAKE_LOCK/VIBRATE/POST_NOTIFICATIONS` manifest, `allowMixedContent` for LAN self-hosts); iOS scaffolded for a Mac session. Runtime backend URL persists to `@capacitor/preferences`; first-run `/setup/backend` gate; wake-lock in cook + shop mode. **Final APK build + on-device browser-verify pending** (no Android SDK on this dev box). FCM push deferred as [[FU-465]]. | [BUILD_NATIVE.md](packaging/BUILD_NATIVE.md) |
| Onboarding | 🟡 | Per-name picks + paste-rows shipped; **verify pending**; preferred-stores step not built (FU-383) | [PROPOSAL](docs/04_proposals/PROPOSAL_ONBOARDING.md) |
| Meal Plans | ➗ | **Built end-to-end** (3 pages, 13 components, board/calendar/templates/shortfall + backend). Waiting on **your screen-style pick + feedback**, not construction | [PROPOSAL](docs/04_proposals/PROPOSAL_MEAL_PLANS.md) + `IMPL_PLAN_MEAL_PLANS_REBUILD.md` |
| Alerts control centre | ✅ | **Fully built**: `/alerts` hub, `ALERT_ROUTER` API, price-watch + email-digest + push delivery. `stocktake_overdue` alert kind now routes through the shared `resolve_overdue_map` (FU-464 housekeeping — was drifting from the runner's authority). | [PROPOSAL](docs/04_proposals/PROPOSAL_ALERTS.md) |
| Assistant surface | ✅ | Per-user rate limits wired (FU-458) — `/ask` 20/min, `/act` + `/confirm` 60/min. `rate_limit` helper extended with a `subject` override so a shared household IP doesn't cross-count users. | `ask_assistant.py`, `auth_helpers.py` |
| Data/Backup admin | ✅ | Collapsed under Settings→Admin→Data; backup library + admin-gating (code committed) | FU-341/342/198 |
| Auth shell | ➗ | Shared `AuthShell.vue` + `AuthButton.vue` across 8 pre-auth surfaces (no standalone register page) | `PROPOSAL_AUTH_SHELL.md` |
| Postgres datastore | ✅ | Implemented + **default** (SQLite fallback via `DORA_DB_PATH`); FU-045 closed | `configuration_manager.py` |
| Recipe importer (paste-based rebuild) | ✅ | **All six chunks landed 2026-07-04.** Parser green on 20/20 corpus; schema migration for unlinked-ingredient tri-state cookability; paste importer replaces the URL fetcher (FU-104 + FU-199 closed); bulk-linker page + PWA share target. Browser-verify pending. | [IMPL_PLAN](docs/04_proposals/IMPL_PLAN_RECIPE_IMPORTER.md) |
| ~~Commercialization~~ → Open-source release (P7) | ⚪ | **Not sold — donation/open-source/all-free (reversed 2026-07-31).** No billing/tenancy code, none planned. Remaining: README/showcase, release process, support channel (FU-406/557). | [PLAN §5](docs/01_charter/RECONCILED_FINISHING_PLAN.md) |
| **Finalisation sweep** | 🔵 | **Designed, not started (2026-07-10; model revised 2026-07-13).** 20 feature chunks, **strict two-stage**: **Stage 1 — Analysis** (read file-by-file; 4 written tracks — FST / help & guides / senior code review / test-plan; the review record is *execution-ready*: exact file:line + change per finding; **no code changed**), then **Stage 2 — Execution** (apply the exact changes from the record — DRY/dead-code/componentisation/placement/naming/standards + verified bugs — test-guarded, **no re-investigation**). **North-star (owner directive):** the app must be hand-maintainable by one person with no AI. Feature-rewrites / contract / large-blast-radius items → new proposal/FU, not in scope. Rolls in FU-361/320/395 (fully), FU-510 Phase 1 + safe Phase-2 swaps, FU-406/404 (partially), FU-010/224 (ride-along). Runs late-game before Phase 4 close-out. | [PLAN](docs/01_charter/FINALISATION_PLAN.md) + [COVERAGE](docs/01_charter/FINALISATION_COVERAGE.md) |

---

## ⚠️ Needs your attention now

Full backlog is **~60 open items** in `DORA_FOLLOWUPS.md`; these are the
ones that want a decision or a running-app check *now*, most important
first.

0. **🔴 BLOCKER — [FU-595](DORA_FOLLOWUPS.md) a past meal you didn't cook FREEZES the whole week.** Found 2026-07-22 walking the planner. If a week holds a past-dated entry with no `consumed_at`, **every** attempt to add a meal to that week returns 400 and **hard-crashes the planner to the error screen**. The server preserves history by `consumed_at is not None` but rejects submissions by `scheduled_for < today`, so a past-but-unconsumed entry can neither be kept by the server nor resent by the client — and the client resends it. Reachable two first-class ways: **auto-drain OFF**, and **reconcile → "Didn't cook"**. Needs a contract decision (preserve past entries regardless of consumption / ignore-rather-than-reject unchanged past entries / stop sending them) — three viable shapes, so it wasn't a safe drive-by fix. The secondary half — a 400 taking down the whole screen instead of a toast — is worth fixing either way. **This also blocks further planner verification:** any week with an unreconciled past meal can't be walked for adds.

0. **✅ RELIABILITY — [FU-549](DORA_FOLLOWUPS_RESOLVED.md) fresh-install migration boot — FIXED 2026-07-13.** The empty-DB `upgrade head` crash (`a3e9f6c2d8b4`, Alembic batch/BINARY) is fixed (UUIDType-column renames pass `sa.BINARY(16)`); the full 116-migration chain now applies cleanly from empty, `alembic` is pinned, and 2 migration tests (from-empty upgrade + schema-vs-model match) now run + guard it. **Residual:** a one-time operator smoke on a real fresh `pip install` (DORA_VERIFY §Operator), and [[FU-553]] — a downgrade-only (`downgrade base`) SQLite CHECK-drop issue that doesn't affect boot.
0. **🔴 SECURITY — unfixed HIGH + MEDIUM findings.** `docs/05_investigations/AUTH_ASSISTANT_SECURITY_FINDINGS.md` records a **HIGH CSRF** flaw and a **MEDIUM email-change** flaw with no fix logged. Surfaced by the 2026-07-02 doc audit — decide whether to fix now before more champion work. Tracked as [FU-447](DORA_FOLLOWUPS.md).
1. **🔴 Meal Plans — pick the screen style + give feedback.** The feature is **built** (board/calendar/templates all shipped); it's waiting on *your* UX-direction call, not on engineering.
2. **🔴 FU-346 — Admin settings "feel hidden."** You raised this. Short direction call needed (stay put / header icon / `/admin` route) before any code moves.
3. **🔴 FU-353 — Rename GitHub repo + local checkout to DashyDora.** Your action (`gh repo rename` + `mv`); until then release-check URLs + README badges 404.
4. **⭐ Verify the champion sequence in-browser.** Four surfaces stacked and untested on this dev box: **P8-07 Zero-Input Pantry** (walk `DORA_VERIFY.md §Stock`); **P8-08 Kitchen health card**; **P8-09 Memory reports**; **P8-10 Native Android APK** (final build + device walk). The stocktake redesign's Chunks 2 + 3 verify blocks in `DORA_VERIFY.md` also want the same walk.
5. **FU-085 — Cookbook tag-taxonomy never run in a real env.** Verify the migration on SQLite + Postgres before building on it.
6. **FU-214/FU-208 — Products-overlay verify mostly cleared (2026-07-24).** My Products + Price History walked on the money seed (mark-inactive, Link…, price chart — with FU-605 fixed en route). What's left is the **FU-606 decision** below and the onboarding-wizard walk; hard-delete is confirmed a deliberate non-feature (soft-deactivate only, ingestion model).
6b. **🔴 FU-606 — quick yes/no: My Products bulk-select.** Confirm the generic "Select on-deal" bulk suffices (recommended) vs. building stock-level-aware variants ("low-stock-on-deal" / "out-of-stock-on-deal"). Recommend **won't-do** — stock-aware buying is already served by Draft-my-shop / buy-verdict / auto-add-on-low.
7. **FU-429 — Assistant-architecture proposal collides with in-flight SLM work.** `DORA_ASSISTANT_ARCHITECTURE_PROPOSAL` proposes a capability registry that the SLM pivot may supersede; needs a reconcile-or-close decision.
7b. **🔴 FU-612 — quick decision: what happens to the `meals_per_week` preference?** The meal-plan builder's count slider was its only consumer and is now gone, so the Settings → Preferences row saves a value nothing reads. Either delete it end-to-end, or repurpose it (e.g. seed the builder's default day selection instead of defaulting to every remaining day).
8. **FU-025 follow-on — component labels ignore the text-scale tokens.** Button/input/toggle labels don't track the A6 scale; small global sweep.

---

## Recently shipped (newest first)

- **Two stock-UI fixes: stocktake fill-pulse + expiry menu wrap (2026-08-10).** Owner-reported. (1) The overdue-Stocktake button's "fill breathes with the glow" (claimed 2026-08-08) never actually rendered: it's an outline button, Quasar forces `background: transparent !important`, and `!important` is ignored inside `@keyframes` — so the fill was dead. Fixed by animating the fill on the `::before` layer (glow stays on the element); confirmed against real Quasar CSS. (2) The expiry shortcut menu was too narrow (`min-width:180px`) so "Push expiry by 14 days" wrapped — widened to 224px + nowrap labels. vue-tsc clean; browser once-over queued.
- **Font picker names the default — "Nunito (Default)" (2026-08-10).** Owner: the font "Default" option should name the actual default so people know what it is. The app's base font is Nunito, so both the Preferences and onboarding font pickers now label it **"Nunito (Default)"**; the redundant standalone "Nunito" option (identical render) was dropped, with a coalesce so a legacy `nunito` selection still shows a chosen option (`nunito` stays a valid stored value). vue-tsc clean; browser-eyeball queued. Spun off FU-614 (font-option list duplicated across the two files — R-003, per-surface Jakarta labels block a naive merge).
- **Recipe cards show an ingredient count (2026-08-10).** Owner CONSIDER. Each recipe card (cookbook grid + stock-item "Recipes using this") gains a small **"N ingredients"** chip beside the time/serves/difficulty chips; singular handled, hidden at zero. Display-only count of the already-loaded ingredient list (R-003 "fine client display math" — no new API field); new `ICONS.ingredients` alias. vue-tsc clean; browser-eyeball queued (no dev server on this machine).
- **Transactional emails get a branded header banner + password-reset copy cleanup (2026-08-10).** Owner feedback on the password-reset email. All transactional emails now open with a brand banner — the Dora mascot on the left and the *Dashy Dora* wordmark in the real Cute Dino font, on the brand-yellow strip — replacing the old plain "Dashy Dora" text heading. Since email clients strip web fonts (and Gmail/Outlook strip `data:` image URIs), the banner is a pre-rendered PNG (rasterised in a browser canvas from the frontend's own font + mascot) embedded as an **inline CID attachment**: `send_email` now wraps the message `multipart/related` with a `cid:brand-banner` image the shared `_layout.html` references (with "Dashy Dora" alt text for graceful degradation; missing-file safe). Password-reset copy: button → **"Reset Password"**, intro → "There was a request to reset the password on…", and the redundant "if you didn't ask for this…" line removed. 30 email tests green; rendered email confirmed in-browser. Durable regenerator committed at `email_templates/assets/generate_brand_banner.py`. Owed: eyes-on in a real inbox once SMTP is live (`DORA_VERIFY.md`).
- **Product Search config simplified — URL in Features, menu button only when set (2026-08-07).** Owner report: the "Product Search" main-menu entry stayed visible with no URL and routed a *normal* user to an admin-only settings page they can't open. Now one rule: set a URL (Settings → System → **Features**) → the entry appears and opens it in a new tab; leave it blank → hidden. Reverted the short-lived dedicated Products settings page + the `product_search_hidden` toggle (page/route/nav row deleted, setting removed end-to-end: entity, DTO, handler, table column, migration `a1b7f4e9c3d5` deleted, API type, dto snapshot). This reverses FU-581's "keep the nav entry visible when unset" call — that was the bug. Verified live (unset → no entry; set → external new-tab entry; DTO drops the field); dto-contract + app-settings + migration + product-search-url tests green, 412 Vitest, vue-tsc/eslint clean. **Dev DBs need a drop_all reset** (deleted migration revision). Residual: one owner eye-check on saving through the Features input itself (API round-trip verified, not the input blur).
- **Meal-plan builder rebuilt around day × meal toggles; duplicate-to-next-week (2026-08-07).** Owner UX call on the "Build my week" first step: the scope segmented (*this week | a day*) + meal-count slider + slot-mode control (*Spread | One slot | Pick slots*) were three overlapping questions answering one. Replaced by two plain rows of card toggles — **Which days** (Mon–Sun with dates, past days disabled *in place* so the row never reflows) and **Which meals** (household slots, applied to all days) — each with Select-all/Clear-all, plus a **Same meals every day** checkbox that builds one day's line-up and repeats it. The count is now derived (`days × slots`, shown as "Dora will plan N meals."), so the slider is gone. Contract rewritten server-side to match: `AutoBuildRequest` is `{days[], slot_names[], repeat_same_day, …}`; `scope`/`start_date`/`meal_count` deleted; placement fills day×slot cells (recipe `time_of_day` still claims its slot). New shared `BaseToggleGroup` component, registered as style-guide **B2a**. Also shipped: **Duplicate to next week** on the desktop + mobile week headers — shifts the week +7 days with **replace** semantics (owner call), confirming the count it will overwrite, then lands you on the copy. Two drive-by fixes called out rather than buried: the mobile Print button was emitting into the void, and the builder's `var(--separator)` borders were a phantom token. Backend ranker tests rewritten (20 green), full backend 1586 pass, 412 Vitest, vue-tsc + eslint clean; behaviour verified live end-to-end. **Layout/visual pass still owed** — the preview browser reported 0×0 for every element in the dialog (including pre-existing controls) and screenshots timed out; queued in `DORA_VERIFY.md`. Spun off FU-611 (small cookbook leaves later days empty), FU-612 (`meals_per_week` pref now drives nothing — owner decision), FU-613 (phantom `--separator` in 4 more components).
- **Settings pages no longer steal input focus mid-save; new D-019 (2026-08-07).** Owner report: you can't edit several settings fields in a row — click from one input to the next and focus is dropped. Cause was not the toast: every settings page bound its in-flight `saving` flag to `:disable` on its own fields (68 bindings across 20 pages), and with blur-triggered autosave, field A's blur disabled field B a frame after the user landed in it — a disabled control can't hold focus. Stock detail was immune because its `busy` flag only gates buttons, the exact contrast the owner spotted. Transient save state now disables no focusable field anywhere in settings; Save/Test buttons keep it as real double-submit protection, and permanent conditions (feature off, SMTP unconfigured, value unchanged) are untouched. Ten pages' bespoke `savingX` refs went dead and were removed. Promoted to **D-019**. lint + vue-tsc clean, 412 Vitest green — but **the live browser pass is still owed** (preview pane wedged; synthetic events never fired a real save), queued in `DORA_VERIFY.md`. Spun off FU-610 (three meal-plan dialogs still violate D-019, low impact).
- **Reconcile "Skip for now" is now pool-neutral — FU-594 (2026-07-31).** Tapping **Skip for now** on a past meal (the passive "ask me later" option) was silently treated like **Didn't cook** — it reversed the nightly auto-drain and cleared the consumed stamp, so skipping a meal *added* portions back to the recipe's available-meals pool (planned-4 entry: pool 10 → 14) and left the pantry over-stating what you have. Owner picked **Option A**: skip is now genuinely neutral (no pool / consumed_at change), matching the module's own long-standing docstring; plus a follow-through fix so a `resolved_deferred` entry reports its true drain — a later Cooked after a skip is a correct no-op and a later Didn't-cook reverses the drain exactly once. Server-side only (state-ownership); 3 new backend contract tests; reconcile + pool + meal-plan suites green (17 + 38). FU-594 → resolved. Meal-planner cluster now down to FU-593 (suggestion visibility).
- **"Build my week" auto-planner replaces the step-by-step builder — FU-596 (2026-07-31).** The meal planner's manual "Plan step-by-step" dialog (hand-pick every recipe; everything dropped into the first slot) is replaced by a real *build-it-for-me* tool: give Dora guidance — plan the week or a single day, how many meals, an **emphasis** (use-up-stock / variety / favourites / surprise), which slots, and (money-on) a keep-under-budget toggle — and she proposes a plan you review and edit (remove/swap/add/reshuffle, per-row slot/servings/day) before saving, with a live "what you'll need" list and a one-tap scoped shopping list. Selection **and** slot placement are server-owned (new `build_week.py`, R-003, mirrors the swap ranker): meals honour a recipe's own `time_of_day` else the least-loaded slot — the FU-596 "all in Breakfast" bug is gone at its root. Reachable from the toolbar, empty-week banner, and a new mobile CTA. 16 backend ranker tests; verified live (week + day, commit, edits); frontend vitest 412 green, vue-tsc/eslint clean. Two residual eyes-on checks queued (budget-cap swap under a real budget; mobile entry point). FU-596 → resolved.
- **Two shopping-list/dashboard bug fixes — FU-573 + FU-585 (2026-07-31).** FU-573: the "remove from every list" toast over-counted (server no-op removals were counted as hits → "Removed from 3 lists" for an item on 1); the endpoint now reports whether it actually removed a line and the toast counts only real removals (backend contract test added, 3 passing). FU-585: the "Log a price" Back arrow now clears the search too, returning to a clean picker (owner call). Also fixed 3 pre-existing stale `addToListButton.spec.ts` assertions; frontend suite green (412) + vue-tsc/eslint clean.
- **"Support Dora" donation buttons + restored GitHub issue/support links — FU-608 (2026-07-31).** Following the open-source pivot: a pulsing pink **Support Dora** heart in the main menu bar, a floating one bottom-left on the sign-in screens, and a pill beside Sign out in Settings — all opening a popover of donation channels (Buy Me a Coffee / GitHub Sponsors / a third TBD). Own `--donate` pink token, accessible name+tooltip, 44px targets, reduced-motion-safe pulse. The GitHub issue/"Report an issue" links stripped when the repo went private are restored (support channel → the public `dashy-dora` issue tracker, auto-lighting Help/error/DoraBot; About page regains Source-code + Report + Support rows; SECURITY.md/README slugs corrected). README rewritten into a real project readme with GIF/screenshot placeholders; `.github/FUNDING.yml` scaffolded. **Donation URLs are placeholders** until the owner stands up the accounts (FU-608 checklist). vue-tsc + eslint clean; auth-shell button + popover verified live.
- **Monetization reversed → donation / open-source, all features free (2026-07-31).** The owner returned to the app's original pre-monetization vision: Dora is **not being sold** — it's donation-funded, open-source (MIT retained), with every feature free to everyone by default. No tiers, no licence key, no billing. The reasoning: a solo-built niche self-host pantry app up against free incumbents (Grocy/Tandoor) has weak paid-revenue potential and a real support burden, whereas donation + open-source costs almost nothing, is more showable, and matches the app's own privacy/anti-creep ethos. **No code changed** (no billing code ever existed). FU-562 (billing) + FU-567 (relicense) resolved won't-do; FU-406/FU-557 reframed to open-source *release* readiness; SaaS/hosted option kept parked. Docs + memory updated.
- **Dashboard money/deal cards fixed on a cold load — FU-586 (2026-07-25).** On a hard reload or PWA cold start, the dashboard's money cards (budget, savings, spend-by-store, pantry value, budget-defense swaps) and the price-drops deal card stayed blank until you navigated away and back. Their loaders gate on the install feature flags, which the app reads once per document from `/api/health`; that probe usually hadn't resolved by the time the dashboard mounted, so the loaders skipped and nothing re-ran them when the flags landed. The dashboard now re-fires exactly those gated loaders when a flag flips on, so the cards fill in on their own; warm navigations are unaffected. No test (the e2e specs that would have pinned it were deleted in the smoke-only pivot) — owner live-walk queued. Off the FU backlog.
- **Owner yes/no batch — FU-602 / FU-606 / FU-604 / FU-603 (2026-07-24).** Cleared four "confirm intent" follow-ups. **FU-602:** the `good_deal` alert + deal-band control were a deliberate descope (band feeds buy-verdict only) — closed, no code. **FU-606:** stock-level-aware bulk variants in My Products are won't-do (anti-creep; that job is Dashboard/buy-verdict) — closed, no code. **FU-604 (fixed):** turning your own money features off now also hides the Alerts price-watch panel (was install-flag-only). **FU-603 (fixed):** the cart add-to-list tooltip no longer says "draft list", and the picker defaults to a planning draft rather than an in-progress shop.
- **Alerts page no longer shows a stale feed — FU-597 (2026-07-24).** It only refetched on open when the store was empty, so once the header bell had populated it, opening Alerts could still show something you'd already resolved elsewhere (planned a meal, restocked an item) until you hit Refresh. It now always refetches on open, matching the bell. Third item off the FU backlog.
- **Settings save-error toasts fixed + de-duplicated — FU-601 (2026-07-24).** A failed settings save used to show a broken toast ("Could not save dora helper shown..") because the success sentence was reused as the error text. Fixed to a clean "Could not save your change." — and since the bug was copy-pasted across all six settings pages, the shared save-toast logic was extracted into one `useSettingsSave()` composable (R-003), deleting the six duplicated copies. Second item off the FU backlog.
- **Meal-planner freeze/crash fixed — FU-595 (2026-07-24).** A past meal you didn't cook (auto-drain off, or "Didn't cook" during reconcile) used to freeze the whole week — every subsequent add failed with "can't schedule in the past" and the error blanked the planner screen. The server now preserves *all* past entries as immutable history (not just consumed ones), the client only ever resends forward-looking entries, and any failed plan write now shows a toast instead of crashing the screen. Pinned by a new backend regression test. First item off the FU backlog now that verification is paused.
- **Verify campaign — History tab, deal-band control, Data pages, Help copy (2026-07-24).** Confirmed the stock-item History tab (the "Opened" entry + expiry trail), the Import/Backup pages render with their wiring intact, and the Help copy matches the Add-item dialog. Also confirmed **FU-602**: the "good deal" alert + its Good/Great deal-band control genuinely aren't in the app (the deal-quality band only feeds the buy-verdict now) — needs an owner call on whether that's the intended descope. Note: the checks an agent can drive on the seed are thinning out; most of what's left in the QA pile needs a human eyeball (visuals, mobile, device/file, fresh install).
- **Verify campaign — Add-item dialog, quick-add, Stock Overview toolbar/expiry (2026-07-24).** Confirmed the Add-a-stock-item dialog (right fields, no expiry field, blank-name and duplicate-name rejection), the quick-add "which list?" flow end-to-end (picker → pick → named toast → remembers the pick for the next add), and the Stock Overview toolbar/footer/bulk-select and expiry controls. No bugs.
- **Verify campaign — Shopping-list UX v2 (2026-07-24).** Confirmed the lists rail (with its "next up" marker), the spend doughnut + totals, the group-by toggle (regroups items by location), bulk-select, and the Print view all work. No bugs.
- **Verify campaign — three cross-cutting sweeps (2026-07-24).** (1) **Dark-mode search-bar contrast (L160)**: every search box (Cookbook, Stock, My Products, Price History, and the Dora chat input) is light text on a dark background in dark mode — no white-on-white anywhere. (2) **Image-source picker (R-024)**: every image-upload surface goes through one shared picker (so the camera/file choice and the resize step are handled in one place), confirmed live on the avatar; corrected two stale checklist notes — the store-logo picker was modernised (no longer a special case) and stock items no longer have an image-edit field (logged FU-607 to confirm that's intended). (3) **Shopping-list receipts (FU-334)**: the Receipts section correctly hides on a draft list, appears once you start shopping (with its empty-state and desktop "Choose receipt" button), and stays after the shop is finished. No bugs.
- **Verify campaign Batch 8 — quick-add toast + "always ask" verified (2026-07-24).** Confirmed the quick-add resolver live: with two draft lists the server asks which list (returns the two candidates), a chosen list adds the item, re-adding reports "already on list", and a stale/finished-list pick is rejected so the app re-asks. The toast wording ("Added to <list name>" / "Already on <list name>"), the picker dialog, the remember-my-pick-for-this-tab behaviour, the "always ask" preference toggle (with its two save messages), and the batch-add-resolves-once behaviour are all confirmed in the code. Only the on-screen picker/toast eyeball is left as an optional owner check (the stock-row cart button doesn't render in the headless pane). No bugs; test data reverted.
- **Verify campaign Batch 8 — trim-to-budget banner reachable states cleared (2026-07-24).** Contrived an over-budget list ($20 weekly budget, $18 of out-of-stock items) and confirmed live: the banner's "Projected · budget remaining · trim $X to fit" copy, the never-cut hold on out-of-stock "buy" items, the "Trimmed everything safe. Still $X over" fallback, and Dismiss. The Keep → Trim-to-fit → Deferred → Add-back happy path stays owner-walk — the running app deliberately can't create a genuinely cuttable line (needs auto-provenance or price-history the API won't set), so it's now documented with that precise reason rather than "needs a cuttable list". No bugs; contrivance torn down.
- **Verify campaign Batch 8 — Put-away dialog cleared (2026-07-24).** Walked the finished-list "Put away" checklist on the seed's done list: the button shows on a done list (not a draft), the dialog groups ticked items by kitchen location with a count, ticking a group collapses it and turns the chip green, "(No location)" pins to the bottom with an Assign button, assigning a location moves the item into its group (and clears the no-location group when empty), Cancel changes nothing, and closing/reopening resets the ephemeral ticks. No bugs; test mutation reverted.
- **Verify campaign Batch 14 — My Products + Price History cleared, one bug fixed (2026-07-24).** On the money seed, walked the two money-gated product surfaces. My Products: mark-inactive no longer 422s, the link/inactive/soft-delete styling + semantics check out, and the "Link…" flow opens its dialog in place (no bounce) and round-trips. Price History: cards, the money-formatted "notify below" input, the deal chip, the full-width graph, and a theme-aware hover tooltip (no more white-on-white) all render. Found and fixed **FU-605**: picking products from the list never updated the chart — selection mutated the list in place under a watcher that only noticed replacement; verified the fix live (add/remove refreshes the chart). Opened **FU-606** (a quick product decision on stock-aware bulk-select — recommend not building it). Test data reverted.
- **Verify campaign Batch 11 — price-watch tier (C-9.5) cleared (2026-07-23).** On the money seed, walked the armed-price-watch surface: the empty state, a watch listed with product/merchant/"notify below $X", View opening the price-history explorer on that product, Remove deleting it with a toast, and the whole panel hiding when the install money flag is turned off. One finding (FU-604): the panel gates on the install money flag while the trim-to-budget banner gates on the per-user opt-out — worth reconciling. Test data reverted.
- **Verify campaign Batch 9 (Cart Button) — money-seed round (2026-07-23).** With the new money-on seed (FU-592), walked the add-to-list cart button end-to-end: the combined QuickAddSheet for items with 2+ linked products (list target + store-offer + quantity in one surface), the silent add for 0/1-product items, the toggle that removes an item already on a list, product-only lines from "Add as product" (tinted + product-chip), and the meal-plan "Generate shopping list" list-picker (cancel / merge into a draft / create a new "Meals: …" list). No bugs; one minor copy finding (FU-603). All test data reverted afterward.
- **Verify campaign Batch 13 (Settings B) opened — CSRF/account block cleared (2026-07-22).** Walked the security-relevant half of the settings surface: CSRF cookie flags, the forbidden-email-field guard, and the CSRF gate proved in both directions (rejected without the header, accepted with it, public login exempt), plus every SPA mutation confirmed to send a header matching the cookie. Also cleared the email-change UI paths with their audit events, the assistant API contracts (no key leak, cross-field validation, master kill-switch both ways), and both FU-285 copy items. One finding: **FU-599** — endpoints with an explicit audit emit also get an automatic one, so account-security actions land in the audit log twice under two different names.
- **Verify campaign Batch 11 (Alerts) opened — history copy bug fixed (2026-07-22).** Walked most of the alerts spine: bell badge, snooze/dismiss propagation, admin thresholds, per-kind disable and tier promote/demote, both forward-looking nudges end-to-end, and the Upcoming fortnight timeline. Fixed a copy bug in the hub's History, which labelled a dismissed out-of-stock alert **"out of_stock"** — the label was built by munging the internal key and only replaced the first underscore; it now reads from the same shared list of alert kinds every other surface uses. Two findings logged for a decision: **FU-597** (the Alerts page can serve a stale feed for a whole session) and **FU-598** (primary and positive are the same colour in the default theme, so categorical dots collide).
- **Verify campaign Batch 7 cont. — planner walk, freeze bug found, print-view "None" fixed (2026-07-22).** Walked the C-2 planner surface and found **FU-595**: a past-day meal you haven't reconciled makes every add to that week fail with a 400 and take the whole planner down to the error screen. Logged rather than patched — the repair is a client/server contract decision. Fixed a second, cosmetic bug in the same round: printing a meal plan titled the sheet literally "None" for the nameless week-plans the planner creates by design; it now reads "Week of <date>", pinned by a new backend test. Cleared meals-per-week 5/5, Print 3/4, useListState 3/4 and the bulk of the C-2 walk. **FU-596** opened (the step-by-step builder files every meal under Breakfast).
- **Verify campaign Batch 7 (Meal plans) — reconcile cleared + a real 404 fixed (2026-07-22).** Walked the reconcile runner end-to-end and found the recap **Done** button navigating to `/dashboard`, a route that does not exist — the final tap of the flow dropped users on the 404 page. Fixed to `/` (the only stale `/dashboard` navigation in the SPA), rebuilt, re-verified. 14/15 reconcile items pass with pool math checked against the API/DB rather than inferred from the UI; Templates drawer went 7/7 and its section was deleted; Show-all-slots 5/6; useListState 4/8. Two findings logged for an owner decision rather than patched: **FU-594** (`skip` reverses the pool drain against its documented contract) and **FU-593** (the reconcile suggestion is invisible behind the 8-item severity-sorted cap). Browser-driving recipe v2 recorded — reloads are safe and unlock the persistence checks recipe v1 could not reach.
- **Verify campaign Batch 0 COMPLETE + 4 bug fixes — FU-571/568/569/572 (2026-07-17).** The three Batch-0 blocker FUs fixed + verified live: **FU-571** — a shared `csrfHeader()` helper swept into every raw-fetch mutating call (chunked uploads, import inspect/commit, backup create/inspect/restore, client-logs, TTS), un-breaking spreadsheet Import + Backup-restore in the browser (header-less calls still 403, so the FU-197 defence is intact); **FU-568** — "a passphrase works well" hint restored on the 4 auth surfaces; **FU-569** — backend console stream re-encoded UTF-8 so Windows cp1252 consoles stop printing a logging traceback per request. Batch 0 then finished in-pane: SettingsFileDrop 5/5, BuyVerdictCard L87–92 all pass, Runtime URL walked. Mid-walk found + fixed **FU-572**: buy-verdict cache invalidation now refetches live cards (they repaint in place instead of staying stale until remount) and the cart quick-add/remove/restock seams now invalidate; "Marked as Well-Stocked." toast de-hardcoded to the real level name. New findings: FU-573 (remove-toast over-count), FU-574 (bogus runtime URL leaves no in-app recovery path). vue-tsc clean; ~9 DORA_VERIFY items ready for owner deletion. Owner picked four heterogeneous items off the 8-item bundle. **Item 6 (menu bottom border)** shipped — removed the redundant `q-header bordered` in `MainLayout.vue` (the feedback asked to *remove* it; toolbar background already separates), vue-tsc/eslint clean, browser-verify queued. **Item 7 (ALDI/IGA logos)** declined WON'T-DO — Dora ships zero logos by deliberate anti-trademark design (`StoreLogo.vue`) and per-store logo **upload** already meets the need (`StoresSettings.vue`). **Item 5 (Kivy P2P sync)** decided CUT — no home in the client-server architecture (serverless multi-master replication cuts against §7.5 + Effortless/Anti-creep; multi-user is already served by the one-instance model), recorded in `MULTI_USER_READINESS.md` §5.1. **Item 2 (telemetry)** designed — new privacy-first `PROPOSAL_USAGE_TELEMETRY.md` (local Surface A recommended core + opt-in aggregate Surface B, gated; hard no on third-party analytics SaaS + content capture, Charter P8); build spawned as FU-566. FU-363 stays OPEN for the remaining four (QA-doc, polish pass, push notifications, UI consistency — all Phase-3/4-timed).
- **Register-barcode-against-Product UI on My Products — FU-373 (2026-07-15).** Closed the last three deferred slices of `PROPOSAL_BARCODE_SCANNING`. Investigation collapsed the scope: the register backend (`POST /api/data/barcodes {barcode, product_id}`, per-product UNIQUE) already existed + tested, and the §6 Data→Scan-tab placement was already resolved (tab removed) — so only the My-Products UI was missing. Added a `scanningEnabled`-gated "Register barcode…" item to the per-card `⋮` menu + a text-entry `BaseDialog` (positive toast on success, inline `describeApiError` incl. 409 "already registered"), mirroring the StockItemDetail add-barcode dialog; whole surface hides when scanning is OFF (R-029). Scope calls made with the user: **My Products only** (no scan-flow fork) and **text entry only** — the P8-02 one-tap scan-unknown→add path is deliberately left untouched (Effortless/Anti-creep), so scan-unknown rework is a **WON'T-DO**. Frontend-only; vue-tsc + eslint clean, Vitest **387/30**. Only §5.2 ingestion auto-populate remains (gated on the Phase-2 ingestion surface — needs an EAN field on Product). Browser-verify queued in DORA_VERIFY.
- **FK on-delete rules reconciled; FU-393 data-model sweep fully closed — FU-565 (2026-07-15).** The sweep's Finding 4: 6 FKs declared an `ondelete` in the model that prod never had (anonymous pre-convention FKs) — a real bug on both engines (SQLite runs `PRAGMA foreign_keys=ON`), e.g. deleting a StockLevel a StockItem referenced errored instead of SET NULL. Migration `d3f8b1a6c4e2` recreates all 6 with the model's rule (SET NULL / CASCADE / RESTRICT), one `batch_alter_table` per table. Navigated the repo's batch double-render trap with `batch.f(...)`; verified rebuilds preserve every other FK + all FU-563 indexes. Extended the schema-match test to also compare FK `ondelete`. Model↔migrated `ondelete` parity now **0**; backend **1490 green**. **This closes FU-393 in full** — the migration gate now enforces tables+columns+nullability+index/unique colsets+FK ondelete.
- **Product nullability reconciled to the model — FU-564 (2026-07-15).** The FU-393 sweep's Finding 3: 3 `Product` columns disagreed on nullability between the model (`create_all`) and prod (`upgrade`). Migration `c1e8a5f3d9b2` tightens `is_active`/`is_available` to NOT NULL (backfilling stray NULLs to true) and loosens `merchant_stockcode` to nullable, via portable `batch_alter_table`. The `Product` rebuild proved clean despite the repo's batch-mode fragility history (preserves rows + `store_id` FK + the FU-563 `ix_Product_store_id` index). `User.username` stays an intentional documented deferral. Removed the 3 Product cols from the schema-match test's allowlist so nullability is now enforced. Backend **1490 green**. Leaves **FU-565** (6 FK `ondelete` drifts) as the sole remaining FU-393-sweep residual.
- **Data-model schema-drift reconciliation + FK covering indexes — FU-563 (2026-07-15).** Actioned the FU-393 sweep's two big findings. The ORM model declared **1** secondary index vs the migrations' **32**, so dev + the whole e2e suite ran a near-unindexed schema unlike prod; and **43 FK columns** had no covering index in prod (parent-delete full-scans + unindexed joins). Mirrored all 32 prod indexes/uniques into `table_mappings.py` (names verbatim, autogenerate stays a no-op) and added the 43 FK indexes to the model + one additive forward-only migration (`b9d4f2a7c3e1`, pure `CREATE INDEX`, portable SQLite+Postgres). Rewrote the schema-match test to compare tables+columns+nullability+index/unique colsets — reflected drift now **0** bar the documented `User.username` deferral. Backend **1490 green** (no test/seed broke on the newly-enforced uniques). Promoted **R-034 + ADR-030** (model owns the whole schema, FK cols indexed by default). Deferred (need batch rebuilds): 6 FK `ondelete` drifts → **FU-565** (confirmed real, not a reflection artifact); 3 Product nullability drifts → **FU-564** (now guarded by the strengthened test's allowlist).
- **SettingsFileDrop accessible refactor — FU-545 (2026-07-15).** Rebuilt the Settings import / backup drop-zone from a `<div role="button">` with a hidden nested file input (a `nested-interactive` axe violation — interactive control inside another) to the standard accessible label-wrap pattern: a non-interactive `<label>` wrapping a single visually-hidden-but-focusable native input the label forwards to natively. Dropped the `role`/`tabindex`/keydown/`inputEl.click()` machinery (and the redundant FU-531 `@click.stop`); focus ring moved to `:focus-within`. Mouse behaviour unchanged; keyboard users now get a focus ring + Enter/Space open. Spec reworked to the new model (13→15) and the **empty + filled axe assertions re-enabled** (held back under FU-542) — all three states scan clean. Frontend Vitest **385→387 / 30**; tree vue-tsc-clean. Parents (`AdminDataImport`/`AdminDataBackupRestore`) untouched — public API unchanged. Browser-walk logged in DORA_VERIFY.
- **`useOfflineQueue` composable spec — FU-520 (2026-07-15).** Drained FU-520 item 3's last opportunistic surface: 13 Vitest cases over the F3 offline mutation queue (`tryWithQueue` swallow-network-else-rethrow, oldest-first `drain` that stops on network errors but shunts non-network 4xx to a conflict pile, reconnect auto-drain, conflict discard/retry, per-user localStorage isolation + corrupt-payload degrade). Frontend Vitest **372 → 385 / 30 files**; full suite green. Testing note captured (singleton composables register never-torn-down module-level watchers — re-mock the reactive seams with a fresh ref per test via `vi.doMock`, not a hoisted `vi.mock`). Test-only; no product code. **FU-520 now has only item 1 left — Postgres CI wiring, gated on FU-405.**
- **Full backend suite runs on Postgres + 2 SQLite-vs-PG divergence bug fixes — FU-520 (2026-07-14).** The suite can now run against a real Postgres (`DORA_TEST_DB=postgres` → dedicated `dora_test` DB via a new `tests/db_backend.py` selector + a Postgres per-test isolation snapshot; SQLite stays the default fast loop). First PG run surfaced **37 failures → 0** tracing to two real runtime bugs a Postgres deployment would hit but SQLite masks: (a) three hand-rolled `text()` UUID binds were dialect-wrong (`bump_pool`, meal-plan `reconcile`, test `uuid_bind`) — a 500 on PG / silent zero-rows on SQLite, now bind `bytes` on SQLite / `str` on PG; (b) an over-length `alert_key` (String(255)) 500s on PG (`StringDataRightTruncation`) — the alert-interaction endpoints now no-op it. Backend green on **both** engines (1490 each). Also shipped the remaining opportunistic frontend specs (shopping-rail 9 + `useMealPlanExport` 3 → frontend **372**) and the companion scraper fixtures (`dora-companion/tests/test_provider_parsers.py`, 8). FU-520 item 1 reduced to just the CI wiring (gated FU-405); item 2 done; only `useOfflineQueue` remains opportunistic.
- **Data-model sanity sweep — FU-393 (2026-07-14).** Ran the P5-08 whole-schema pass (nullability / FK-consistency / index coverage / dead columns) as one dedicated investigation ([`DATA_MODEL_SANITY_SWEEP_FU393.md`](docs/05_investigations/DATA_MODEL_SANITY_SWEEP_FU393.md)), diffing the model (`create_all`) vs migrated (`upgrade`) schemas. Surfaced real drift: the model builds **1** secondary index vs prod's **32** (dev/test run near-unindexed ≠ prod), **42** FK columns unindexed in prod (35 CASCADE/SET NULL), and 3 unintentional `Product` nullability mismatches — plus the schema-match test only checks table names. Read-only; remediation spawned as FU-563 (index drift + FK indexes + test hardening) + FU-564 (nullability). FU-393 resolved.
- **Production WSGI server (gunicorn) — FU-397 (2026-07-14).** The self-host container now serves the API through gunicorn instead of Flask's dev server (table-stakes for a credible release). `startup.sh` picks the server env-driven (`DORA_API_SERVER=auto` → gunicorn when `DORA_ENV=production`); new `gunicorn.conf.py` binds to the same `:5170` nginx upstream with a **single `gthread` worker by default** — deliberate, because the in-process scheduler must fire once (>1 worker warns loudly; horizontal scale stays parked in the optional-deployment doc). `startup.py` split into a reusable `bootstrap()` + a new `dora_api/wsgi.py` entry point; dev/desktop/test paths unchanged. e2e 1006 + top-level 484 green, 9 new unit tests. Worker-split half was already relocated, so FU-397 is fully resolved.
- **Action-first Stock Overview scan mode — FU-378 (2026-07-14).** Last open item on `PROPOSAL_STOCK_OVERVIEW.md` built. The Scan button (gated on `scanning_enabled`, off by default) opens the camera with an **always-visible current-action chip you can switch without leaving the camera**. Default = "Open stock item" (scan → jump to the item, then closes); switch to **Set to <level>** per stock level → the camera stays open and sets each scanned item to that level via the shared `updateStockLevelAsync` (R-003 — optimistic + offline-queue + auto-add), with a per-item confirm banner — one unified scanner that doubles as a fast stocktake (switch the target level on the fly). Unknown/unlinked codes are reported + skipped, not routed into add (that stays FU-373). Pure logic in `helpers/scanActions.ts` (8 new unit tests). PROPOSAL_STOCK_OVERVIEW: every §7 decision now resolved.
- **Commercialization split: self-host-first, SaaS/managed parked — (2026-07-14).** Owner decision to **sell Dora self-hosted** first. New `docs/04_proposals/OPTIONAL_SAAS_AND_MANAGED_DEPLOYMENT.md` is the "revisit later" home for multi-tenant (Path A) + managed single-tenant (Path B) + scale/billing work. Relocated FU-400/401/399/398/403/402 there; split FU-397 (WSGI stays / worker-split parked), FU-406 (self-host launch stays / on-call parked), FU-412 (scoped to self-host plan). COMMERCIALIZATION_REPORT §5/§7 annotated. Distribution-posture insurance (Decision 5) untouched, so reopening the hosted path stays a project not a rewrite. Docs only.
- **Email setup verified + FU-413 closed — (2026-07-14).** Confirmed the INV-4 email/forgot-password proposal already shipped (via FU-333 operational-config): admin SMTP config lives on `AppSetting` (encrypted password), the sender degrades to dry-run when unconfigured, and the login page hides "Forgot password?" via the pre-auth `email_sender_configured` capability. Verified end-to-end at runtime (dry-run → false; username set → true). No new code — stale FU closed; the proposal's "refuse-enable-without-creds" guard was superseded by degrade-to-dry-run, and Phase-3 presets stay parked (Anti-creep).
- **Seed builders DRYed — FU-556 (2026-07-14).** The builder closures + fixed vocab + harvest loop that `seed.py` and `seed_showcase.py` each carried their own copy of now live once in `seed_builders.py` (`SeedBuilders(repo, now)`). Both seeds bind its methods to the local names they already used, so datasets + call sites are byte-identical — verified: dev seed row-counts identical to pre-refactor baseline, showcase matches its FU-392 counts, e2e 1006 + top-level 475 green.
- **Migration-test isolation fixed — FU-561 (2026-07-14).** Two `test_migrations.py` tests failed only in the full top-level run — root cause was `test_sqlalchemy_repository.py` leaking `DORA_DB_URL` at module scope (collection time), which outranks `DORA_DB_PATH` and sent the migration child process to the wrong DB. `_run_migration` now drops `DORA_DB_URL` from the child env. Top-level suite green again: **475 passed** (was 473 + 2 failed).
- **P5-03 leftover perf slices — FU-560 (2026-07-14).** Frontend bundle-size analysed → **healthy** (route-level code-splitting works; the sole >500 KB chunk is the lazy /reports echarts page, never in initial load — no action). Backend `AppSetting` singleton now memoised on Flask app-context `g` (`access.py`) → `/api/health` 5→2 queries, `/api/alerts` 11→10; full e2e suite 1006 green. Spun FU-561 (pre-existing migration-test isolation failure, verified on clean HEAD).
- **Perf & scale sweep — FU-388 (2026-07-14).** Ran the P5-03 sweep against the loaded seed (profiled heavy read endpoints at 500 vs 2000 items). **Result: clean — no N+1s;** query counts identical across both loads on every endpoint (recipe cookability is set-based, not per-recipe). One fix shipped: `/api/health` re-read the `AppSetting` singleton ~4×/probe → now fetched once (5→3 queries on a client-polled endpoint). Writeup in `docs/05_investigations/PERF_SCALE_SWEEP_FU388.md`; remaining axes (frontend bundle-size, request-scoped settings memoization) spun as FU-560. Also fixed FU-559 (pre-existing import-template test).
- **Dev seed runs under load by default — FU-388 (2026-07-14).** The dev seed now appends a configurable bulk load (`DORA_SEED_BULK_ITEMS`, default **500** extra stock items + proportional products/offers, level-change + price history, ~62 recipes, one big shopping list) so every interactive dev session runs at realistic scale and N+1s surface naturally. Reuses the curated builder closures (no drift); env-gated; the e2e suite passes 0 so its boot stays fast (seeded 521 items in 0.54s). This is the *enabler* for the P5-03 perf sweep — the actual N+1 profiling/fix pass remains under FU-388. Also logged FU-559 (pre-existing, unrelated import-template test failure found mid-run).
- **Products-overlay proposal residuals closed — FU-376 (2026-07-14).** Doc-hygiene sweep of `PROPOSAL_PRODUCTS_AS_OVERLAY.md`: its §7 "open build-time decisions" block walked against shipped code and closed (gate = `Product.count()>0` ✅; URL-unset nav now hidden via R-029/FU-500, superseding the old reveal-disable rec; search opens in a new tab; `PreferredBuy` shipped as an FK). GAP bucket swept — the one real gap (L205/206 bulk-select) already lives in open FU-214; §11's data-presence-gating ADR held for a 2nd use. No code change.
- **Support / "Report an issue" channel — FU-370 (2026-07-14).** Built the code half of `PROPOSAL_SUPPORT_CHANNEL.md`, **off by default**: a Help-page "Report an issue" button, a wired error-screen "Report this" button, and a DoraBot report link all appear once a channel is configured, opening a public issues repo / form / mailto with a pre-filled subject+body. Honest one-person/side-project Help copy replaces the old passive line. Per owner steer the target is a **hardcoded, author-controlled switch + env override** (`support_channel.py`), **never** an admin setting — the proposal's AppSetting/admin-editor is withdrawn (out of R-030 scope by design; settled FU-558). Standing up the real channel is FU-557. vitest 335 green.
- **Import-template registry symmetry guard — FU-348 (2026-07-14).** `_validate_import_templates()` now enforces the invariant *both* ways at boot: FU-350 already proved every template has a commit path; FU-348 adds the reverse (every commit-known section must ship a downloadable template) so a future recipes/shopping-list importer can't silently miss its "Download template" button. Minimal module-load assertion (not the speculative registry-of-registries — deferred). +1 direct-import symmetry test.
- **Demo / sellable-showcase mode — FU-392 (2026-07-13).** `DORA_DEMO_MODE=true` turns any install into a self-resetting product demo: boots a *curated* showcase dataset (`seed_showcase.py` — clean pantry/recipes/meal-plan/shopping story, no dev test artifacts; login `demo`/`demo`), auto re-seeds on an interval (`DORA_DEMO_RESET_MINUTES`, default 60), and the SPA shows a persistent "Demo mode — resets periodically" pill (pre-auth `demo_mode` capability). The **shared** cut (one live install, one dataset) — this is the intended design; per-visitor isolation was considered and **closed won't-do** (FU-555, 2026-07-14): all visitors share the data + interval reset, no demo multi-tenancy. Backend auth-flows 31 green; SPA vue-tsc clean + vitest 329.
- **Filter/sort field allowlist made strict — FU-546 (2026-07-12).** A list filter/sort on a non-column field (relationship name, internal attr) now 400s instead of 500ing — `_resolve_field` restricted to genuine mapped columns; dead `validate_known_fields` removed. Completes the FU-537 hardening at one choke point. Suite 1454 green.
- **Playwright first run GREEN + 2 real bugs caught — FU-540/550/551 (2026-07-12).** Got the browser E2E smoke suite running green (9 tests, driving system Chrome via `DORA_E2E_CHANNEL` since the bundled binary won't download here). Standing it up immediately caught two "can't ship" bugs, both fixed: **FU-550** — the SPA hadn't been buildable since ~June 22 (`quasar build` type-check failed on a `draft_shop` card missing from the `CardId` union — a deploy blocker); **FU-551** — Windows self-host served `.js` as `text/plain`, so the browser-served SPA never booted (blank page). backend 1454 / frontend 337 / e2e 9, all green.
- **Logout is now audited — FU-548 (2026-07-12).** Session termination was the one mutating endpoint with no audit trail; the logout handler now emits an `auth.logout` event naming the actor (before clearing the session), mirroring login. FU-538 sweep test flipped to assert it. Suite green.
- **Browser E2E smoke layer (Playwright) — FU-540 (2026-07-12).** Built as a *complement* to manual DORA_VERIFY (user's call — not a replacement). Single-origin setup (backend serves API + built SPA), seeded throwaway backend via webServer, 9 smoke tests (login good/bad + authed nav sweep + /api handshake), npm scripts + README. Config + specs parse-validated (`--list`); **first real browser run pending** — `playwright install chromium` is network-blocked in the dev sandbox, so it needs a run on CI / a networked machine (DORA_VERIFY §Operator). This closes the LAST test-suite FU.
- **Concurrency / idempotency tests — FU-535 (2026-07-12).** `test_concurrency.py` (5 tests, deterministic interleaving): double-submit create is name-deduped (422, not a dup), re-applied level drop records consumption once (offline-replay idempotency), disjoint PATCHes don't clobber, delete-then-reference 4xx's cleanly. No bugs — behaviours sound. **🏁 This closes the last writable test-suite FU** — 534/535/536/537/538/539/541/542 all done this session; only FU-540 (Playwright, needs a decision) remains. Suite 1449→1454.
- **authStore Vitest spec — FU-520 (2026-07-14).** `authStore.spec.ts` (17 tests) pins the cold-boot probe (shared in-flight promise, fresh-install /me skip, parked-promise retry loop with the network-vs-generic message split), the FU-355 logout + silent-401 → clear-user + `clearAllListState` contract, and the avatar cache-bust. Frontend suite → **360 / 27 files**. Was the flagged "strongest remaining single-store candidate"; FU-520's two other remnants (Postgres CI, scraper fixtures) stay gated/cross-repo.
- **Migration integrity tests — FU-536 (2026-07-12).** `tests/test_migrations.py`: single-head / linear-history / real-downgrade checks (in-process) + up/down round-trip and schema-match (isolated subprocess DB). **Surfaced a ⚠️ possible fresh-install boot failure — FU-549:** migrations were never run in tests (create_all is used), and `upgrade head` from empty crashes at `a3e9f6c2d8b4` (Alembic batch/BINARY, alembic unpinned). Production fresh-boot runs upgrade-from-empty, so a new self-host may not boot on the release toolchain — needs confirmation + fix before Postgres (FU-045) / advertising fresh self-host. Suite 1446→1449.
- **Audit-coverage sweep — FU-538 (2026-07-12).** `test_audit_completeness.py` (7 tests) pins every mutating endpoint is audited-or-explicitly-exempt (structural, ~169 pairs) + drives 18 mutations end-to-end asserting actor + scrubbed payload. Audit is a single global hook → surface is fully covered; the one un-audited mutating endpoint (`logout`) is flagged for a product decision (FU-548). Suite 1439→1446.
- **Security test suite — FU-537 (2026-07-12).** `test_security_injection.py` (11 tests): filter/sort injection stays literal (never SQL), privilege/id fields can't be mass-assigned, user content is HTML-escaped in emails. Found + fixed a real bug — crafted filter field names referencing internal Python attributes (`__class__`) returned 500 (and leaked which attributes exist via 400-vs-500); now a clean 400. Residual dead-allowlist wiring = FU-546; token-lifecycle tests = FU-547. Suite 1428→1439.
- **Frontend resilience test suite — FU-539 (2026-07-12).** 33 tests over the SPA safety-net layer (offline queue, rollback registry, global error handler, error boundary, unsaved-changes guard) — suite 304→337. Found + fixed a real robustness bug: a single throwing rollback used to strand every remaining optimistic-update undo; each is now isolated. `useNetworkStatus` (timer singleton) intentionally left.
- **Query-count / N+1 regression guards — FU-534 (2026-07-12).** `test_query_budgets.py` pins that the stock list, dashboard summary, shopping-list detail, and cookable-recipe filter each issue a *constant* number of SELECTs regardless of data volume (verified: 4 / ~15 / batched / batched) — the automated net for R-032, so the noload/N+1 pattern behind several of this month's bugs now fails a test instead of shipping silently. Suite 1424→1428. Opportunistic tail (search / meal-plans / reports) deferred per-touch.
- **Frontend a11y testing wired — FU-542 (2026-07-12).** `vitest-axe`/`axe-core` + a shared `expectAccessible()` helper, wired into AlertRow / DoraModeSlider / SettingsFileDrop specs (suite 298→304). First run caught + fixed a real defect (unlabelled hidden file input); the remaining `nested-interactive` on that component is logged as FU-545 (needs a label-wrap refactor). Fanning axe out to more component specs is now a one-line import.
- **Two engineering rules written + the uuid-converter sweep applied — R-032/R-033, FU-532 (2026-07-12).** User signed off the two recurring-pattern ADRs: **R-032/ADR-028** (noload include-discipline) and **R-033/ADR-029** (uuid path-param converter), both now in `ENGINEERING_STANDARDS.md`. Applied R-033 as FU-532: **123 entity-id path params across 61 endpoints** now use Flask's `<uuid:...>` converter, so a malformed ID in a URL 404s at routing instead of 500ing — closing the last of the str/UUID family (handler layer FU-528 + routing layer FU-532). Non-UUID params left alone; 5 handler-parse routes deferred as a carve-out ([[FU-544]]); bonus dead-code cycle-detection fix in the locations tree. Suite 1424 passed, path-param xfail gone.
- **Batch of pinned finding-level bugs fixed — FU-527/528/524/522 (2026-07-12).** Six production sites: the "keeps running out" report no longer drops history-less items; stock-group `item_count` shows the real count (was always 0); tool/dietary-tag delete now reports the real "recipes affected" (str/UUID key fix — **the whole FU-528 str/UUID family is now closed**); `normalise_unit` handles degree marks idempotently; and the change-email confirmation email's plain-text link points at the correct `/confirm-email-change` route. Each pin flipped to a regression; suite 1423 passed. Opened **FU-543** to promote the two recurring-pattern ADRs (noload include-discipline; uuid path-param converter) — both await product-owner sign-off.
- **FU-526 CLOSED — stocktake snooze no longer 500s the queue / alerts bell on SQLite (2026-07-12).** `resolve_overdue_map` compared a tz-naive (SQLite) `snoozed_until` against a tz-aware `now_` → `TypeError` → `GET /api/stocktake/queue` 500'd, and the alerts feed shared the map so the bell went down too, for as long as any item was snoozed. Fixed by coercing to aware-UTC before the compare (one site). Regression test + alerts-feed health assertion; suite 1420 passed. Also wired **FU-541** frontend coverage reporting (`npm run test:coverage`, no gate). Observation: the naive/aware coercion is now inlined 10+ places → a shared `ensure_utc()` helper is the obvious FU-510 de-dupe.
- **FU-533 CLOSED — noload-PATCH family fixed, incl. the silent data-loss bug (2026-07-12).** Cook-mode / sourced stock-level drops were recording **no `ConsumptionEvent`** (depletion history silently lost) and same-level confirms logged phantom history rows — both because the handler read a `lazy="noload"` `stock_level` off a plain load; fixed with `.include(STOCK_LEVEL)`. Also fixed recipe cuisine/category/collection explicit-null clears (write underscore FK directly). All 3 pins flipped to regressions; full suite 1419 passed. Consumption browser walk queued. Also logged **FU-541** (coverage reporting — no % gate) + **FU-542** (frontend a11y/axe tests) to the backlog. The noload include-discipline ADR (FU-527) is still awaiting sign-off.
- **Test-suite deep-dive — mapped the missing test *types*, wrote FU-linked stubs (2026-07-12, later).** No tests written / no code touched (deliberate, to conserve usage). Identified 7 wholesale-missing test categories and left green skipped stubs with full briefs for the next agents: **FU-534** query-count/N+1 budgets (do-first — the recurring noload family's regression net), **FU-535** concurrency/idempotency, **FU-536** migration round-trip/portability (117 migrations, zero tests), **FU-537** security (filter-parser SQLi / token single-use / stored-XSS / mass-assignment), **FU-538** audit-coverage sweep, **FU-539** frontend resilience layer (offline queue / rollback / error boundary), **FU-540** browser-E2E infra decision (Playwright, logged not stubbed).
- **Test sweep pass 4 — API fuzz + PATCH semantics + Pinia stores + e2e tail closed; 7 bugs found & fixed (2026-07-12).** Backend 1323 → **1415 passing** (~61 s), frontend 254 → **298** (19 files). New: whole-API input-fuzz sweep ("4xx never 500" over malformed bodies / garbage query + path params / wrong-type fields), PATCH-semantics suite (59 — null-out matrix, no-op, unknown-field, cross-field 4xx across 9 surfaces), suggestions + substitutes routers (FU-519 tail closed; deals has no HTTP surface), and the Pinia store layer (stockItem/shoppingList/alert/location, +44). **Seven real bugs found, all fixed inline:** `recipes/unlinked-ingredients` 500'd on every request (FK field-name outlier `stock_item_id` vs `_stock_item_id`), `meal-plans/reconcile-queue` 500'd on pagination (SQLite str `scheduled_for` vs `.isoformat()`), and **rename-to-own-name 422'd at 5 surfaces** (UUID-vs-str self-exemption — every edit dialog broke on re-save). Two logged for architecture calls: **FU-532** (82 routes 500 on a non-UUID path param — uuid-converter ADR) and **FU-533** (three noload-read PATCH bugs incl. silent consumption-event loss). **Two ADRs flagged DUE** (noload include-discipline; uuid path-param converter).
- **Test sweep pass 3 — enforcement sweeps + delete integrity + FU-519 tail + component layer; product-unlink bug found+fixed (2026-07-11).** Backend 1238 → **1323 passing** (~53 s), frontend 203 → **254** (15 files). New: route auth/permission enforcement sweep (anonymous 401 over all 271 /api rules + pinned 32-endpoint admin-403 contract + allowlist-rot and reverse call-site guards — **no vulnerabilities found**), delete/referential-integrity suite (24 cross-entity delete webs), locations-tree/app_settings/client_logs/help e2e (55), and 4 component specs (AddToListButton 20 / AlertRow 13 incl. the unknown-kind degrade regression / DoraModeSlider 6 / SettingsFileDrop 12). **One real user-visible bug found + fixed: product unlink from a stock item always 404'd** (str path param vs UUID compare — third FU-528-family incident; browser walk queued). Digest dedup test made date-robust (seeded `stocktake_overdue` crossing the pinned June window — root-caused, contract-true rewrite); enforcement sweep made order-robust (in-memory rate-limiter drain). FU-530 (duplicate admin gate copy) + FU-531 (component nits) logged.
- **Test sweep pass 2 — +142 backend / +11 frontend tests; FU-519 essentially closed (2026-07-10, later).** Search router e2e (scoring order, per-type subtitles, filter semantics), DTO contract snapshots over 27 endpoints (`dto_snapshots.json`, refresh-via-env-var workflow), 103 long-tail e2e tests across waste/budget/reports/stocktake + the six taxonomy routers, and the first component-level Vitest (both StockLevelDot components; the Quasar mount pattern — client-bundle alias + plugin-vue + per-file jsdom — is now established for future component specs). Four more real findings logged without scope creep: **FU-526 (stocktake queue 500 + broken alerts bell while a snooze is active, SQLite naive/aware compare — fix next)**, FU-527 (two noload-reads-as-empty count bugs; third noload incident today, ADR candidate), FU-528 (str/UUID delete-count mismatches), FU-529 (reconcile receipt ordering lacks a deterministic tie-break; transient idempotence flake observed and root-suspected). Suite: 1238 passed / 5 strict-xfail pins / 1 pre-existing fail, ~45 s; frontend 203, ~1.5 s.
- **FU-519/FU-520 targeted test sweep — +176 backend / +139 frontend tests, cookbook-filter production bug found+fixed (2026-07-10).** Backend pytest 920 → 1096 passing (~41 s); frontend Vitest 53 → 192 (~1 s). New suites: recipe/meal-plan/dashboard router e2e (search still open on FU-519), `SqlAlchemyRepository` operator/paginate/field-map/NOCASE suite on a standalone fixture, emailer template-render + fake-SMTP send-path (29 tests), Hypothesis property invariants over recipe_cookability/stock_status/product_offer/units (26 tests), and 9 frontend spec files over the pure utils/composables. The recipe e2e suite exposed a real user-visible bug — **the cookbook `?cookable` / `?max_missing` / `?expiring_within_days` filters were silent no-ops** (plain id-universe load before the eager cookability/expiring maps left identity-mapped `ingredients` empty, so `cookable=true` kept everything and `cookable=false` returned an empty page) — fixed in `_restrict_query` with the trap documented in place, pinned by four tests, browser walk queued in DORA_VERIFY. Four findings logged (FU-522 change-email plain-text wrong link, FU-523 Contains/StartsWith `case_sensitive` inversion, FU-524 `normalise_unit` non-idempotence, FU-525 buy-verdict UTC/household clock mix — the test-side version of which flaked every AEST morning and was fixed). FU-518 closed: digest dedup test rewritten to per-alert-key assertions; `GetAlertsHandler.handle()` gained a `now` seam. Commented-out CI updated to run bare `pytest` + `npm test` on re-enable (FU-405).
- **Alerts bell crash fixed + Dashboard toast parity + FU-357 closed (2026-07-10).** User's browser walk on FU-357 hit a real `ErrorBoundary` crash on the bell after pushing a few expiries. Root cause: FU-317 Chunk 4 (2026-07-09) added `meal_reconcile_overdue` on the backend but never extended the SPA's `AlertKind` union, so TS couldn't enforce the five kind-switches — all fell through to `undefined` and `AlertRow.vue:114`'s `.slice()` threw. Fixed by extending the union + all five switches (icon `playlist_add_check`, link `/meal-plans/reconcile`, nav-only nudge) + a defensive `?? []` in AlertRow so the next kind rollout can't crash. Same session found and fixed a second real drift — Dashboard's alert-action was silently succeeding (no `$q.notify` while AlertsBell + AlertsPage both toasted "Done."); parity restored. **FU-357 closed** — the reported "cross-app undo seems off" turned out to be this toast inconsistency, not an undo (no undo exists on the push_expiry path anywhere in the SPA, confirmed by grepping every `label: 'Undo'` site). New **FU-521** logged for the state-ownership drift (three hand-rolled alert-action wrappers + five parallel kind-switch lists) — folds into finalisation plan Chunk 9 senior review.
- **Finalisation plan drafted + 3 FUs closed by absorption (2026-07-10).** New [FINALISATION_PLAN.md](docs/01_charter/FINALISATION_PLAN.md) + [FINALISATION_COVERAGE.md](docs/01_charter/FINALISATION_COVERAGE.md) at `docs/01_charter/`. 20 feature chunks × 4 output tracks (Full Systems Test procedure with an 8-persona × device × condition matrix / in-app help & guides / senior code review / test plan inventory) built up in the same code-walk per chunk. **FU-361 A-4 help content + FU-320 auto-behaviour documentation + FU-395 P5-11 production readiness review CLOSED** — full scope absorbed into the plan (moved to `_RESOLVED`; plan is the sole tracker for their content). **FU-510 hand-rolled-vs-library Phase 1** rolled into Track 3 as a per-chunk `keep / replace / wrap-thin-adapter` bucket, Phase 2 stays open. **FU-406 launch readiness + FU-404 P7-08 compliance** stay open with partial-coverage notes (FST doc + persona flows exercise the QA/DSAR halves; marketing/legal/on-call/DSAR-drafting stay in their own FUs). **FU-010 holistic theme review + FU-224 colour-usage assessment** stay open, riding along — every chunk's senior review logs candidate sites against them so the eventual eyes-on-app pass starts with a triaged shortlist. No code touched.

---

## Where the detail lives

- **`DORA_WORKLOG.md`** — per-session handoff narrative (what ran, decisions, what's next).
- **`CHANGELOG.md`** — product/code changes that shipped.
- **`DORA_FOLLOWUPS.md`** — the full ~60-item open backlog (this dashboard shows only the top).
- **`DORA_VERIFY.md`** — your browser-verify checklist (walk + delete as you confirm).
- **The full per-doc register is below** — every planning doc's verified state.
- **Charter / how & why:** `docs/01_charter/` (vision, standards, master plan).
- **To refresh this doc:** see the regeneration routine in `CLAUDE.md`.

**Do not trust as current** (kept for history only): the old `STATUS.md`
(retired to `06_legacy_prompt_plans/`), `99_scratch/PROGRESS_REPORT_2026-06-12.md`
and `FEEDBACK_AUDIT_2026-06-12.md` (June snapshots — say Phase 2/3 = 0%, both wrong
now), `docs/00_DOC_GRAPH.md` (stale, FU-428), and `00_original_spec/` (historical,
pre-dates the current codebase).

---

# Document register

Complete per-doc state map — every active planning doc opened, classified, and
cross-checked against `DORA_WORKLOG.md` + `CHANGELOG.md` + git (verified
2026-07-02). This is the "everything accounted for" backing for the dashboard
above; the dashboard is the rollup, this is the per-doc truth. **You don't need to
read this** — it's the audit trail. 107 active docs across 7 folders; the 157
`docs/00_original_spec/` files are charter-designated historical (one bucket, see
end).

State key: ✅ done-clean · ➗ done-with-carve-outs · 🟡 active · 🔵 designed-not-built ·
⚪ not-started · 🕸 stale · 📦 superseded (successor named) · 🗄 historical.
Investigations: ✅ closed-actioned · 🟡 open · 🔵 informational · 🕸 stale.

## Systemic findings (from the 2026-07-02 audit)

1. **🕸 `docs/00_DOC_GRAPH.md` is stale (FU-428/446).** Indexes ~25 of 48 proposals; cites 2 files that don't exist. Now shrunk to a stub.
2. **🕸 Stale "no code yet" headers on ~15 shipped docs (FU-445).** Bodies are accurate records; only the top status line lies. Judge by this register.
3. **🔴 Security (FU-447).** `AUTH_ASSISTANT_SECURITY_FINDINGS` has an unfixed HIGH CSRF + MEDIUM email-change flaw.
4. **~23 docs orphaned** from the (now-retired) indexes — real, mostly-shipped records.

## 01_charter — governance (5)

| Doc | Type | State | Purpose | Evidence |
|---|---|---|---|---|
| DASHY_DORA_CHAMPION_PLAN.md | Charter | 🟢 authoritative | Vision + 12-principle Decision Charter + P8 prompts | Cited library-wide as arbiter |
| ENGINEERING_STANDARDS.md | Standards | 🟢 authoritative | Code/architecture rubric R-001..R-026 + ADR log | Enforced by CLAUDE.md close-gate |
| RECONCILED_FINISHING_PLAN.md | Master-plan | 🟢 authoritative | 5-phase order, scope, §7 resolved decisions | Self-maintaining |
| FINALISATION_PLAN.md | End-plan | 🔵 designed, not started (2026-07-10; model revised 2026-07-13) | End-of-project sweep, 20 chunks, **strict two-stage** (Stage 1 analysis → execution-ready written findings; Stage 2 execution from the record, no re-investigation) under the single-maintainer north-star; FU rollup for FU-361/320/395/510/406/404/010/224 | Two-stage model 2026-07-13 |
| FINALISATION_COVERAGE.md | Register | 🔵 blank (2026-07-10) | Per-chunk × per-track status matrix for the finalisation plan | Companion to FINALISATION_PLAN.md |

## 02_feedback — inputs (3)

| Doc | State | Purpose | Evidence |
|---|---|---|---|
| Feedback _ Fixes - as of [06-Jun-2026].md | 🟢 authoritative | Raw user feedback — source of truth | Named SoT in CLAUDE.md |
| COVERAGE_GAPS.md | 🟡 active-living | Tracker: feedback bullets lacking a home | Updated through 2026-06-16 |
| FEEDBACK_TRIAGE_AND_PLAN.md | 📦 superseded (partial) | Feedback→work map | Superseded for sequencing by RECONCILED plan |

## docs/ root — navigation & guides

| Doc | State | Purpose | Notes |
|---|---|---|---|
| 00_DOC_GRAPH.md | 🕸 stale→stub | Per-prompt required-reading map | Shrunk to a stub (FU-428/446) |
| INGESTION_GUIDE.md | 🟢 authoritative | Admin guide: sourcing data via /api/ingest | Matches shipped C-10.x |

## 03_prompts — executable prompts (19)

| Doc | State | Evidence |
|---|---|---|
| 00_INDEX.md | 🕸 stale (banner added) | Status column pre-dates shipped code |
| A1 / A1b / A3 / A6 / A8 | ✅ done | Theme, token-tuning, modal, text-size, renames — all shipped |
| A2 / A4 / A5 / A7 | ➗ carve-outs | Button, filter, skeleton, footer — shipped w/ opportunistic tails (FU-006/011-013/023/024) |
| B1 / B3 / B4 / B5 / B7 / B8 | ✅ done | All bug clusters shipped (CHANGELOG B-series) |
| B9 | ➗ carve-outs | Shipped; item 4 (command palette) deliberately cancelled |
| C_big_rock_design_briefs.md | 🔵→built | 14 briefs → proposals; most built via IMPL_PLANs |
| INV_investigations.md | 🔵→done | INV-1..8,10 produced memos; INV-9 superseded |

## 04_proposals — designs, impl-plans, runbook (49)

| Doc | State | Notes |
|---|---|---|
| PROPOSAL_STOCK_OVERVIEW / _MEAL_PLANS / _COOK_MODE / _COOKBOOK / _CART_BUTTON / _ALERTS / _INGESTION_API / _CONFIG_AND_OPTINS | ✅ done | Big-rock C-1/2/3/4/7/9/10/cross designs — all built via IMPL_PLANs |
| PROPOSAL_STOCK_OVERVIEW / _AUTH_SHELL / _BUY_VERDICT_ORACLE | ✅ done | Auth-shell + buy-verdict shipped 2026-07-02 (⚠️ stale headers, FU-445) |
| PROPOSAL_STOCK_ITEM_DETAIL / _ONBOARDING / _BARCODE_SCANNING / DORA_ASSISTANT_ARCHITECTURE | ➗ carve-outs | Mostly shipped; named deferrals |
| PROPOSAL_COOKBOOK_CARD_REVISION / _RECIPE_IMAGE_STEPS / _WASTE_MINIMISATION / _SHOPPING_LIST_UX_V2 | ✅ done | Shipped; were orphaned from indexes |
| STATE_OWNERSHIP_REFACTOR_PROPOSAL | ✅ done | R-003 authority; IMPL executed |
| PROPOSAL_PRODUCTS_AS_OVERLAY / IMPL_PLAN_PRODUCTS_AS_OVERLAY / PRODUCTS_OVERLAY_RUNBOOK | 🟡 active | Phase F in progress; RUNBOOK is the ⭐ live driver |
| PROPOSAL_HELP_OVERLAY | 📦 superseded | Overlay mechanism retired 2026-07-06 → executed as targeted `(?)` chips per `IMPL_PLAN_HELP_CHIPS.md` (FU-503 / FU-044 both resolved) |
| PROPOSAL_SUPPORT_CHANNEL | ➗ built (dormant) | FU-370: code half shipped off-by-default; hardcoded author-controlled switch, never an admin AppSetting (settled, FU-558; §4.1/§4.2 withdrawn). Channel stand-up = FU-557 |
| PROPOSAL_TEST_SUITE_IMPROVEMENTS | ➗ carve-outs | Built across ~9 sessions (FU-371 resolved 2026-07-13); Phases 1-3 done, Phase 4 (FU-520) all but item 1. `useOfflineQueue` spec (2026-07-15) drained item 3's last surface; frontend Vitest 385/30. Sole carve-out left: **Postgres CI + coverage gate, blocked on CI-off (FU-405)** — suite already green on PG via `DORA_TEST_DB` selector. (scraper tests live in companion repo, FU-161) |
| IMPL_PLAN_HELP_CHIPS | ✅ done | Executed 2026-07-06 (FU-503) |
| PROPOSAL_LOCALE_I18N | ➗ carve-outs | Layers A + B shipped 2026-07-06 (FU-043); Layer C (full UI translation) explicitly parked as someday |
| PROPOSAL_SIMPLE_MODE | 📦 superseded | → products-as-overlay |
| SHOPPING_LIST_REDESIGN_PROPOSAL | 📦 superseded | v1 shipped (P6-01) → UX_V2 presentation |
| IMPL_PLAN_* (Alerts, Cart, Cookbook, Cook-Mode, Dashboard, Error-Handling, Ingestion, Meal-Plans, Meal-Plans-Rebuild, State-Ownership, Stock-Item-Detail, Stock-Overview, Waste, Your-Prices, Settings-Rebuild, Shopping-Lists, Shopping-List-Receipts, Config, Auth-Shell) | ✅ done | All executed & shipped. ~13 carry stale "no code yet" headers (FU-445). MEAL_PLANS_REBUILD is the live meal-plans authority. |
| IMPL_PLAN_RECIPE_IMPORTER | 🔵 designed | 2026-07-04. Six-chunk paste-based rebuild; supersedes IMPL_PLAN_COOKBOOK §Chunk 7's URL-importer scope. Closes FU-104 / FU-199 / FU-396 (importer half). |
| PLAY_STORE_LISTING | 🔵 designed | 2026-07-04 (P8-10). Play Store copy draft, 6-shot screenshot plan, adaptive-icon note. **No submission** — reference for the day one happens. |
| PROPOSAL_STOCKTAKE_MODE | ✅ done | 2026-07-04. Full stocktake-mode redesign — decisions-locked brief AND shipped end-to-end (Chunks 1–3 backend engine + SPA runner + Settings/Overview surfacing + housekeeping). Anchor for FU-226/430 close-out; SK-1..11 feedback resolved. |
| PROPOSAL_MEAL_PLANS_PART_2 | ✅ built | 2026-08-11. **Cook batches** — one cook feeds several days. **BUILT END-TO-END + verified live** (FU-617 resolved). Backend: `CookBatch` entity/table/migration `d5a9f27c4e18`, `cook_key` grouping+validation, derived read view. Manual UI: "Cook · serves N"/"Leftovers" markers + link/unlink menu + day-picker, gated on the Batch cook-style. Builder: "Build my week" proposes cooks (one recipe per 3-day run for Batch households; ties off FU-611). `test_cook_batches.py` 10 + `test_build_week.py` +3; consolidated gate 59 green; verified live (18 markers → 6 CookBatch rows). Proposal Phases 2/3 dropped as no-ops (Σ-yield batch-invariance, pinned by test). One light owner-walk queued (the manual day-picker dialog). (same recipe+slot, distinct days). First-class `CookBatch` + `MealPlanEntry.cook_batch_id`; `cook_key` grouping on the write path; batch = demand unit (counted once at total yield), entry = consumption unit; 6 aggregation impact points listed; reconcile cook-once-drain-many; per-entry linked-cook UI (no layout rebuild); builder auto-proposals for Batch households (also the honest FU-611 fix). Folds into the existing `batch_features_enabled` cook-style. Open decisions all closed; grounded in a 4-agent code investigation. Build = **FU-617**. No code yet. |
| PROPOSAL_MEAL_RECONCILE | 🔵 designed | 2026-07-09. "Stocktake mode for meals" — per-user auto-drain setting + new `/meal-plans/reconcile` surface + `MealPlanReconcileReceipt` audit table + `meal_reconcile_overdue` alert. Anchor for FU-317 close-out (F5/F6 magic-audit resolution). **Decisions locked; four residuals answered in the impl-plan.** No code yet. |
| IMPL_PLAN_MEAL_RECONCILE | 🟡 in-progress | 2026-07-09. Six-chunk executable plan. **Chunks 1-5 ✅ shipped 2026-07-09** — schema + receipt writer + sweep rewrite (Ch 1); R-003 pool-helper collapse (Ch 2); queue + verb endpoints (Ch 3); `meal_reconcile_overdue` alert + `reconcile_meals_pending` suggestion sharing one `reconcile_overdue_signal` authority (Ch 4); **UX shipped Ch 5** — `/meal-plans/reconcile` runner page + dashboard `ReconcilePastMealsChip` + meal-plans header nudge + `useReconcileQueue.ts` composable. **Chunk 6** (settings row + copy polish + COVERAGE_GAPS flip) is the last thing pending. |

## 05_investigations — reports (18)

| Doc | State | Notes |
|---|---|---|
| DATA_MODEL_SANITY_SWEEP_FU393 | ✅ done | P5-08 whole-schema sweep (2026-07-14). Found model↔migration index drift (1 vs 32), 42 unindexed FK cols, 3 Product nullability mismatches → remediation FU-563/564. Read-only. **Fully remediated 2026-07-15** — FU-563 (index drift closed + 43 FK indexes), FU-564 (Product nullability), FU-565 (6 FK ondelete); schema-match test now enforces tables+columns+nullability+index/unique+FK-ondelete (R-034/ADR-030). No residual |
| PERF_SCALE_SWEEP_FU388 | ✅ done | P5-03 perf sweep — no N+1s; `/api/health` query fix; residual axes → FU-560 |
| AUTH_ASSISTANT_SECURITY_FINDINGS | 🟡 open | 🔴 HIGH CSRF + MEDIUM email-change unfixed (FU-447) |
| STOCK_OVERVIEW_PERF / ORPHANED_FIELDS_AUDIT / FEATURE_CLARIFICATIONS / MAGIC_BEHAVIOUR_AUDIT / PLATFORM_BUILDS_AUDIT / Distribution Spec | 🟡 open | Findings stand; tracked (FU-411/415/416/417/418) |
| EMAIL_SETUP_FINDINGS (INV-4) | ✅ shipped | Proposal (admin SMTP-on-AppSetting + hide forgot-password when unconfigured) shipped via FU-333; verified + closed FU-413 (2026-07-14) |
| COMMAND_PALETTE / ESSENTIAL_FLAG / HISTORY_TAB / LOGGING_AND_DATA_LAYOUT / RECIPE_COMPARISON | ✅ closed | Recommendations actioned |
| COMMERCIALIZATION_REPORT / MULTI_USER_READINESS | 🗄 historical (monetization) | **Selling reversed 2026-07-31 → donation/open-source/all-free.** The report's whole monetization thread (billing/tiers/relicense/freemium) is now historical; its productionization findings (§3–4) still stand + shipped. FU-400/401/399/398/403/402 stay parked in OPTIONAL_SAAS below |
| SELF_HOST_COMMERCIALIZATION_PLAN (04_proposals) | 📦 superseded | **Superseded 2026-07-31** (banner added) — sequenced the paid tracks; FU-562 (billing) + FU-567 (relicense) RESOLVED won't-do. Residual = open-source *release* readiness (FU-406/557) |
| OPTIONAL_SAAS_AND_MANAGED_DEPLOYMENT (04_proposals) | 🔵 deferred option | "Revisit later" bucket for multi-tenant SaaS (Path A) + managed single-tenant (Path B) + scale/billing. Even less likely post-pivot (donation/OSS) but **kept parked, not closed** (owner, 2026-07-31). Created 2026-07-14 |
| SUBSTITUTE_SWAP_ASSESSMENT | 🕸 stale | Rec obsoleted by Shop-Mode merge |

## 06_legacy_prompt_plans — historical (8)

All 🗄 historical & correctly retired: `PROMPT_PLAN.md`, `PROMPT_PLAN_PART_2..7`,
`STATUS.md` (self-labels retired 2026-05-27). Superseded by `03_prompts/`.

## 99_scratch — raw notes (5)

| Doc | State | Recommendation |
|---|---|---|
| PROGRESS_REPORT_2026-06-12 / FEEDBACK_AUDIT_2026-06-12 | 🗄 historical | Delete — superseded by this doc (kept briefly for FU cross-refs) |
| PRICING_SYSTEM_REASSESSMENT_HANDOFF | 📦 superseded | Executed into IMPL_PLAN_YOUR_PRICES; archive/delete (FU-425) |
| SENIOR_REVIEW_2026-06-16 | 🗄 historical | Keep — still cited by open FUs |
| MINIMAL_USER_PRODUCTS_OFF_FRICTION | 🗒 untriaged | Only genuinely untriaged note — promote or keep |

## docs/00_original_spec — historical bucket (157 files)

The author's first spec (Feature Boards + ~125 "I can …" notes + original plan).
Charter-designated **historical / non-authoritative** — pre-dates the current
codebase; overridden by charter/plan/feedback. Mined opportunistically when writing
a brief. Treat the whole folder as 🗄 historical; not verified per-file.
